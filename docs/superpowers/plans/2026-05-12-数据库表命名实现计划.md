# 数据库表命名规范实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成数据库表名规范化的第一阶段：代码层对齐——服务层文件改名、表名常量映射、引用更新、scope 兼容代码清理

**Architecture:** 采用两阶段策略，本计划仅覆盖 Phase 1（代码层对齐）。新增 `src/services/db/tables.js` 集中管理表名常量，逐一重命名服务层文件（保留兼容导出），更新所有 composables 中的引用路径，清理 summary 模块的遗留兼容代码。

**Tech Stack:** Vue 3 (Composition API + `<script setup>`), Supabase, JavaScript

---

### 前置准备

**Related files:**
- `src/services/db/` — 所有待改名的服务层文件
- `src/services/database.js` — 主入口，需要同步更新
- `src/views/` — 各模块 composables 引用

- [ ] **Step 1: 确认当前线上数据库结构**

Run: Supabase MCP list_tables 确认线上表结构与当前代码一致

Expected: 与 `current-structure.md` 快照无冲突

- [ ] **Step 2: 阅读当前 habit_logs 相关代码了解直接引用情况**

```bash
grep -n "habit_logs" src/services/db/habits.js
```

Expected: 找到所有 `client.from('habit_logs')` 调用位置，确认后续替换为表名常量

---

### Task 1: 新增表名常量映射文件

**Files:**
- Create: `src/services/db/tables.js`

- [ ] **Step 1: 创建 tables.js**

```javascript
/**
 * ============================================
 * 数据库表名常量映射 (services/db/tables.js)
 * ============================================
 *
 * 集中管理所有业务表名，避免表名散落在各服务模块中。
 * Phase 2 数据库改名后只需更新此文件中的常量值。
 */
export const TABLES = {
  // Goals / Direction
  GOAL_CATEGORIES: 'plans_category',
  GOAL: 'plans',
  GOAL_MONTHS: 'monthly_plans',
  GOAL_DAYS: 'daily_plans',

  // Tasks
  TASK: 'tasks',

  // Habits
  HABIT: 'habits',
  HABIT_LOGS: 'habit_logs',

  // Summary
  SUMMARY: 'summaries',

  // Day
  DAILY_REPORT_LOG: 'daily_report_views'
}
```

- [ ] **Step 2: 验证文件创建成功**

Run: `node -e "require('./src/services/db/tables.js')"` 或 `pnpm vue-tsc --noEmit`（预期无报错，因为还没被引用）

---

### Task 2: 清理 summaryAdapters.js 中 scope 兼容代码

**Files:**
- Modify: `src/views/summary/utils/summaryAdapters.js`

- [ ] **Step 1: 移除 legacyScopeToKind 映射表和 resolveKind 中的 scope 路径**

替换 `legacyScopeToKind` 定义及其引用，移除 `resolveKind` 中的 `summary.scope` 分支，简化函数为只处理 `kind`：

```javascript
const resolveKind = (summary) => {
  if (typeof summary.kind === 'string') return summary.kind
  throw new Error('Summary payload is missing kind')
}
```

同时删除文件顶部的 `legacyScopeToKind` 常量定义（第 56-61 行）。

- [ ] **Step 2: 检查是否有其他文件引用 legacyScopeToKind**

```bash
grep -rn "legacyScopeToKind" src/
```

Expected: 只在本文件中，无外部引用

- [ ] **Step 3: 运行类型检查**

Run: `pnpm vue-tsc --noEmit`
Expected: 无报错

---

### Task 3: 服务层文件改名 + 表名常量化（Direction 模块）

**Files:**
- Rename: `src/services/db/plans.js` → `src/services/db/goal.js`
- Rename: `src/services/db/monthlyPlans.js` → `src/services/db/goalMonths.js`
- Rename: `src/services/db/dailyPlans.js` → `src/services/db/goalDays.js`
- Rename: `src/services/db/plansCategory.js` → `src/services/db/goalCategories.js`
- Modify: `src/services/db/goal.js` — 导出名改为 `goal`，表名用常量
- Modify: `src/services/db/goalMonths.js` — 导出名改为 `goalMonths`，表名用常量
- Modify: `src/services/db/goalDays.js` — 导出名改为 `goalDays`，表名用常量
- Modify: `src/services/db/goalCategories.js` — 导出名改为 `goalCategories`，表名用常量

- [ ] **Step 1: 重命名文件**

```bash
mv src/services/db/plans.js src/services/db/goal.js
mv src/services/db/monthlyPlans.js src/services/db/goalMonths.js
mv src/services/db/dailyPlans.js src/services/db/goalDays.js
mv src/services/db/plansCategory.js src/services/db/goalCategories.js
```

- [ ] **Step 2: 修改 goal.js**

导入 `TABLES` 常量替换 `createBase('plans')`，导出名改为 `goal`：

```javascript
import client from '@/config/supabase'
import { TABLES } from './tables'

const supabase = client.createBase(TABLES.GOAL)

export const goal = {
    async list() {
        return await supabase.query(q => q
            .select('*, plans_category(id, name)')
            .order('priority', { ascending: false })
            .order('created_at', { ascending: false })
        )
    },
    async create(plan) {
        return await supabase.create(plan)
    },
    async update(id, updates) {
        return await supabase.update(id, updates)
    },
    async delete(id) {
        return await supabase.delete(id)
    }
}
```

注意：`list()` 中的 `plans_category` 是 join 关联查询的目标表名，Phase 2 之前保持不变（Phase 2 改表名后再改这里）。

- [ ] **Step 3: 修改 goalMonths.js**

```javascript
import client from '@/config/supabase'
import { TABLES } from './tables'

const supabase = client.createBase(TABLES.GOAL_MONTHS)

export const goalMonths = {
    async list(planId) {
        return await supabase.query(q => {
            let query = q.select('*').order('month', { ascending: true })
            if (planId) query = query.eq('plan_id', planId)
            return query
        })
    },
    async create(plan) {
        return await supabase.create(plan)
    },
    async update(id, updates) {
        return await supabase.update(id, updates)
    },
    async delete(id) {
        return await supabase.delete(id)
    }
}
```

注意：`eq('plan_id', planId)` 中的字段名在 Phase 2 再改为 `goal_id`。

- [ ] **Step 4: 修改 goalDays.js**

```javascript
import client from '@/config/supabase'
import { TABLES } from './tables'

const supabase = client.createBase(TABLES.GOAL_DAYS)
const plansBase = client.createBase(TABLES.GOAL)

// ... toDateOnly, subtractDays, getMaxCarryOverLookbackDays 保持不变 ...

export const goalDays = {
    async list(monthlyPlanId) {
        return await supabase.query(q => {
            let query = q.select('*').order('day', { ascending: true })
            if (monthlyPlanId) query = query.eq('monthly_plan_id', monthlyPlanId)
            return query
        })
    },
    async create(plan) {
        return await supabase.create(plan)
    },
    async update(id, updates) {
        return await supabase.update(id, updates)
    },
    async delete(id) {
        return await supabase.delete(id)
    },
    async deleteByIds(ids) {
        if (!Array.isArray(ids) || ids.length === 0) return []
        return await supabase.query(q => q
            .delete()
            .in('id', ids)
        )
    },
    async listByDate(date) {
        const dateStr = toDateOnly(date)
        return await supabase.query(q => q
            .select(`
                *,
                monthly_plans (
                    id,
                    task_time,
                    duration,
                    plans (
                        id,
                        task_time,
                        duration
                    )
                )
            `)
            .eq('day', dateStr)
        )
    },
    async listForDayView(date) {
        // ... 内容不变，仅将 plansBase 改为 client.createBase(TABLES.GOAL) ...
        const targetDateStr = toDateOnly(date)
        const maxLookbackDays = await getMaxCarryOverLookbackDays()
        if (maxLookbackDays === 0) return await this.listByDate(date)
        const earliestDateStr = toDateOnly(subtractDays(date, maxLookbackDays))
        const rows = await supabase.query(q => q
            .select(`
                *,
                monthly_plans (
                    id,
                    task_time,
                    duration,
                    plans (
                        id,
                        task_time,
                        duration,
                        carry_over_lookback_days
                    )
                )
            `)
            .gte('day', earliestDateStr)
            .lte('day', targetDateStr)
            .order('day', { ascending: true })
        )
        return (rows || []).filter((plan) => {
            if (plan.day === targetDateStr) return true
            if (plan.status !== 0) return false
            const lookbackDays = Number(plan.monthly_plans?.plans?.carry_over_lookback_days || 0)
            if (lookbackDays <= 0) return false
            const earliestAllowed = subtractDays(date, lookbackDays)
            const planDate = new Date(`${plan.day}T00:00:00`)
            return planDate >= earliestAllowed && plan.day < targetDateStr
        })
    }
}
```

注意：join 查询中的 `monthly_plans` 和 `plans` 子查询表名在 Phase 2 再改。

- [ ] **Step 5: 修改 goalCategories.js**

```javascript
import client from '@/config/supabase'
import { TABLES } from './tables'

const supabase = client.createBase(TABLES.GOAL_CATEGORIES)

export const goalCategories = {
    async list() {
        return await supabase.query(q => q
            .select('*')
            .order('name', { ascending: true })
        )
    },
    async create(category) {
        return await supabase.create(category)
    },
    async update(id, updates) {
        return await supabase.update(id, updates)
    },
    async delete(id) {
        return await supabase.delete(id)
    }
}
```

- [ ] **Step 6: 验证四个文件无语法错误**

Run: `node -e "
  require('./src/services/db/goal');
  require('./src/services/db/goalMonths');
  require('./src/services/db/goalDays');
  require('./src/services/db/goalCategories');
"`

Expected: 无报错（注意需要 babel 或 esm 支持，若失败则用 `pnpm vue-tsc` 替代）

---

### Task 4: 服务层文件改名（Tasks 模块）

**Files:**
- Rename: `src/services/db/tasks.js` → `src/services/db/task.js`
- Modify: `src/services/db/task.js` — 导出名改为 `task`，表名用常量

- [ ] **Step 1: 重命名文件**

```bash
mv src/services/db/tasks.js src/services/db/task.js
```

- [ ] **Step 2: 修改 task.js**

```javascript
import client from '@/config/supabase'
import { TABLES } from './tables'

const supabase = client.createBase(TABLES.TASK)

export const task = {
    async list(start, end) {
        return await supabase.query(q => {
            let query = q.select('*').order('start_time', { ascending: true })
            if (start) query = query.gte('start_time', start.toISOString())
            if (end) query = query.lte('start_time', end.toISOString())
            return query
        })
    },
    async create(task) {
        return await supabase.create(task)
    },
    async update(id, updates) {
        return await supabase.update(id, updates)
    },
    async delete(id) {
        return await supabase.delete(id)
    },
    async listInbox() {
        return await supabase.query(q =>
            q.select('*').eq('status', 'inbox').order('created_at', { ascending: false })
        )
    }
}
```

- [ ] **Step 3: 验证**

Run: `pnpm vue-tsc --noEmit`
Expected: 无报错

---

### Task 5: 服务层文件改名（Habits 模块）

**Files:**
- Rename: `src/services/db/habits.js` → `src/services/db/habit.js`
- Modify: `src/services/db/habit.js` — 导出名改为 `habit`，client.from('habits') 替换为常量，client.from('habit_logs') 替换为常量

- [ ] **Step 1: 重命名文件**

```bash
mv src/services/db/habits.js src/services/db/habit.js
```

- [ ] **Step 2: 修改 habit.js**

将 `createBase('habits')` 和所有 `client.from('habit_logs')` 替换为表名常量：

```javascript
import client from '@/config/supabase'
import { trackGlobalLoading } from '@/composables/useGlobalLoading'
import { TABLES } from './tables'

const supabase = client.createBase(TABLES.HABIT)

export const habit = {
    async list() {
        return await supabase.query(q => q
            .select('*')
            .order('created_at', { ascending: true })
        )
    },
    async listLite() {
        return await supabase.query(q => q
            .select('*')
            .order('created_at', { ascending: true })
        )
    },
    async listLogsByDate(startDate, endDate) {
        return await trackGlobalLoading(async () => {
            const { data, error } = await client
                .from(TABLES.HABIT_LOGS)
                .select('*')
                .gte('completed_at', startDate.toISOString())
                .lte('completed_at', endDate.toISOString())
            if (error) throw error
            return data
        })
    },
    async listLogsByHabit(habitId) {
        return await trackGlobalLoading(async () => {
            const { data, error } = await client
                .from(TABLES.HABIT_LOGS)
                .select('*')
                .eq('habit_id', habitId)
                .order('completed_at', { ascending: true })
            if (error) throw error
            return data
        })
    },
    async listLogsByYear(year) {
        return await trackGlobalLoading(async () => {
            const startDate = new Date(year, 0, 1).toISOString()
            const endDate = new Date(year, 11, 31, 23, 59, 59).toISOString()
            const { data, error } = await client
                .from(TABLES.HABIT_LOGS)
                .select('*')
                .gte('completed_at', startDate)
                .lte('completed_at', endDate)
                .order('completed_at', { ascending: true })
            if (error) throw error
            return data
        })
    },
    async create(habit) {
        return await supabase.create(habit)
    },
    async update(id, updates) {
        return await supabase.update(id, updates)
    },
    async delete(id) {
        return await supabase.delete(id)
    },
    async log(habitId, log = '', completedAt = null) {
        return await trackGlobalLoading(async () => {
            const payload = {
                habit_id: habitId,
                log
            }
            if (completedAt) {
                payload.completed_at = completedAt
            }
            const { data, error } = await client.from(TABLES.HABIT_LOGS).insert(payload).select().single()
            if (error) throw error
            return data
        })
    },
    async deleteLog(logId) {
        return await trackGlobalLoading(async () => {
            const { error } = await client.from(TABLES.HABIT_LOGS).delete().eq('id', logId)
            if (error) throw error
        })
    }
}
```

- [ ] **Step 3: 验证**

Run: `pnpm vue-tsc --noEmit`
Expected: 无报错

---

### Task 6: 服务层文件改名（Summary 和 Day 模块）

**Files:**
- Rename: `src/services/db/summaries.js` → `src/services/db/summary.js`
- Rename: `src/services/db/dailyReportViews.js` → `src/services/db/dailyReportLog.js`
- Modify: `src/services/db/summary.js` — 导出名改为 `summary`，表名用常量
- Modify: `src/services/db/dailyReportLog.js` — 导出名改为 `dailyReportLog`，表名用常量

- [ ] **Step 1: 重命名文件**

```bash
mv src/services/db/summaries.js src/services/db/summary.js
mv src/services/db/dailyReportViews.js src/services/db/dailyReportLog.js
```

- [ ] **Step 2: 修改 summary.js**

```javascript
import client from '@/config/supabase'
import { trackGlobalLoading } from '@/composables/useGlobalLoading'
import { mapSummaryRowToRecord } from '@/views/summary/utils/summaryAdapters'
import { TABLES } from './tables'

const table = TABLES.SUMMARY

const persistSummary = async (payload) => {
  const query = payload.id
    ? client.from(table).update(payload).eq('id', payload.id)
    : client.from(table).insert(payload)

  const { data, error } = await query.select().single()
  if (error) throw error
  return mapSummaryRowToRecord(data)
}

export const summary = {
  async listByKind(kind) {
    return await trackGlobalLoading(async () => {
      const { data, error } = await client
        .from(table)
        .select('*')
        .eq('kind', kind)
        .order('period_start', { ascending: false })
      if (error) throw error
      return (data || []).map(mapSummaryRowToRecord)
    })
  },
  async save(payload) {
    return await trackGlobalLoading(async () => persistSummary(payload))
  },
  async remove(id) {
    return await trackGlobalLoading(async () => {
      const { error } = await client.from(table).delete().eq('id', id)
      if (error) throw error
    })
  }
}
```

- [ ] **Step 3: 修改 dailyReportLog.js**

```javascript
import client from '@/config/supabase'
import { TABLES } from './tables'

const supabase = client.createBase(TABLES.DAILY_REPORT_LOG)

export const dailyReportLog = {
  async getByUserAndDate(userId, reportDate) {
    if (!userId || !reportDate) return null
    const rows = await supabase.query(q =>
      q.select('*').eq('user_id', userId).eq('report_date', reportDate).limit(1)
    )
    return rows && rows.length ? rows[0] : null
  },
  async create(payload) {
    return await supabase.create(payload)
  }
}
```

- [ ] **Step 4: 验证**

Run: `pnpm vue-tsc --noEmit`
Expected: 无报错

---

### Task 7: 更新主入口 database.js

**Files:**
- Modify: `src/services/database.js`

- [ ] **Step 1: 更新导入语句和导出对象**

```javascript
import { goal } from './db/goal'
import { goalMonths } from './db/goalMonths'
import { goalDays } from './db/goalDays'
import { habit } from './db/habit'
import { task } from './db/task'
import { summary } from './db/summary'
import { goalCategories } from './db/goalCategories'
import { dailyReportLog } from './db/dailyReportLog'

export const db = {
  rpc(name, params) {
    const result = client.rpc(name, params)
    return result
  },
  goal,
  goalCategories,
  goalMonths,
  goalDays,
  habit,
  task,
  summary,
  dailyReportLog
}
```

同时更新文件头部的 JSDoc 注释中的表名描述。

- [ ] **Step 2: 验证**

Run: `pnpm vue-tsc --noEmit`
Expected: 无报错

---

### Task 8: 更新 Direction 模块 composables 引用

**Files:**
- Modify: `src/views/direction/composables/useDirectionBatch.js`
- Modify: `src/views/direction/composables/useDirectionFetch.js`
- Modify: `src/views/direction/composables/useDirectionGoals.js`

- [ ] **Step 1: 搜索所有旧引用**

```bash
grep -rn "db\.plans\|db\.plansCategory\|db\.monthlyPlans\|db\.dailyPlans\|dailyPlansCache\|monthlyPlansCache" src/views/direction/
```

- [ ] **Step 2: 更新 useDirectionBatch.js**

将所有 `db.dailyPlans` 替换为 `db.goalDays`，`dailyPlansCache` 替换为 `goalDaysCache`，`monthlyPlansCache` 替换为 `goalMonthsCache`：

```javascript
import {
  goalMonthsCache,
  goalDaysCache,
  selectedGoal,
  selectedMonth,
  // ...
} from '@/views/direction/composables/useDirectionSelection'

// ...

const getExistingDailyPlanMap = async (goalMonthId) => {
  if (!goalDaysCache[goalMonthId]) {
    await loadDailyPlans(goalMonthId, { force: true })
  }
  const existingDailyPlans = goalDaysCache[goalMonthId] || []
  // ...
}

// 方法体内部：
await db.goalDays.update(existingDailyPlan.id, { ... })
await db.goalDays.create(payload)
await db.goalDays.deleteByIds(idsToDelete)
```

- [ ] **Step 3: 更新 useDirectionFetch.js 和 useDirectionGoals.js**

查找这两个文件中是否有对 `db.plans`、`db.monthlyPlans`、`db.dailyPlans`、`db.plansCategory` 的引用，替换为对应的新导出名：

- `db.plans` → `db.goal`
- `db.monthlyPlans` → `db.goalMonths`
- `db.dailyPlans` → `db.goalDays`
- `db.plansCategory` → `db.goalCategories`

Grep 确认后再改，避免遗漏：

```bash
grep -n "db\.\(plans\|monthlyPlans\|dailyPlans\|plansCategory\)" src/views/direction/composables/useDirectionFetch.js src/views/direction/composables/useDirectionGoals.js
```

- [ ] **Step 4: 验证**

Run: `pnpm vue-tsc --noEmit`
Expected: 无报错

---

### Task 9: 更新 Day 模块 composables 引用

**Files:**
- Modify: `src/views/day/composables/useDayData.js`
- Modify: `src/views/day/composables/useDayExecutionItems.js`（join 查询中的表名引用，Phase 2 再改，但确认无硬编码引用路径）
- Modify: `src/views/day/composables/useDailyReport.js`

- [ ] **Step 1: 更新 useDayData.js 中的引用**

查找 `db.habits` 替换为 `db.habit`：

```bash
grep -n "db\.habits" src/views/day/composables/useDayData.js
```

替换为 `db.habit.listLogsByDate`、`db.habit.log`、`db.habit.deleteLog`、`db.habit.listLite`。

- [ ] **Step 2: 更新 useDailyReport.js 中的引用**

查找 `db.dailyReportViews` 替换为 `db.dailyReportLog`：

```bash
grep -n "db\.dailyReportViews" src/views/day/composables/useDailyReport.js
```

替换为 `db.dailyReportLog.getByUserAndDate` 和 `db.dailyReportLog.create`。

- [ ] **Step 3: 检查 useDayExecutionItems.js**

这个文件的 join 查询字段（`plan.monthly_plans?.plans?.task_time` 等）引用的是**查询结果的属性名**，不是直接的表名。只要服务层 `goalDays.listByDate` 返回的数据结构不变，这里无需修改。确认：

```bash
grep -n "monthly_plans\|daily_plans" src/views/day/composables/useDayExecutionItems.js
```

这些是 join 查询结果的字段路径。Phase 2 数据库改名后 Supabase 会自动映射新表名到结果字段，届时再检查是否需要调整。

- [ ] **Step 4: 验证**

Run: `pnpm vue-tsc --noEmit`
Expected: 无报错

---

### Task 10: 更新 Summary 模块 composables 引用

**Files:**
- Modify: `src/views/summary/composables/useSummaryManager.js`

- [ ] **Step 1: 更新引用**

查找 `db.summaries` 替换为 `db.summary`：

```bash
grep -n "db\.summaries" src/views/summary/composables/useSummaryManager.js
```

替换为 `db.summary.listByKind`、`db.summary.save`、`db.summary.remove`。

- [ ] **Step 2: 验证**

Run: `pnpm vue-tsc --noEmit`
Expected: 无报错

---

### Task 11: 搜索剩余旧引用

**Files:**
- 全项目搜索

- [ ] **Step 1: 搜索所有 db.plans / db.monthlyPlans / db.dailyPlans / db.plansCategory / db.tasks / db.habits / db.summaries / db.dailyReportViews 的剩余引用**

```bash
grep -rn "db\.\(plans\|monthlyPlans\|dailyPlans\|plansCategory\|tasks\|habits\|summaries\|dailyReportViews\)" src/ --include="*.js" --include="*.vue"
```

Expected: 如果前面任务都完成了，应该只有测试文件中的引用（`src/services/__tests__/`），以及 Phase 2 才需要处理的 join 查询字符串。

- [ ] **Step 2: 如果有遗漏，逐个更新**

---

### Task 12: 更新 current-structure.md

**Files:**
- Modify: `database/current-structure.md`

- [ ] **Step 1: 全量更新文档**

使用最新的 Supabase MCP 查询结果重写 `database/current-structure.md`，包括：
- 更新时间改为当天
- 所有表名更新为目标名（Phase 1 阶段，表名常量中的值仍是旧表名，但文档可以使用新名称描述）
- 移除 `daily_summaries` 引用
- 补充备份表说明
- 更新数据行数

- [ ] **Step 2: 最终验证**

Run:
```bash
pnpm vue-tsc --noEmit
pnpm build
```

Expected: 无报错无警告
