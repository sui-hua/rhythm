# 所向页面默认展示当月 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 所向页面进入和切换目标时默认展开当前月，当前月不在目标范围内时展开该目标的第一个月。

**Architecture:** 在 `useDirectionFetch.js` 内新增默认月度计划解析 helper，统一 `fetchData()` 和 `selectedGoal` watch 的月份选择逻辑。通过 `useDirectionFetch.spec.js` 覆盖“当前月存在”和“当前月不存在”两条路径，避免两个入口后续行为漂移。

**Tech Stack:** Vue 3 Composition API, Vitest, Supabase service wrapper

---

## 文件结构

- 修改: `src/views/direction/composables/useDirectionFetch.js`，新增默认月度计划解析 helper，并替换初始加载与目标切换中的重复选月逻辑。
- 修改: `src/views/direction/composables/__tests__/useDirectionFetch.spec.js`，补充默认月份选择的单元测试。

---

### Task 1: 抽取默认月度计划选择 helper

**Files:**
- Modify: `src/views/direction/composables/useDirectionFetch.js`

- [ ] **Step 1: 在 `isInitializing` 下方新增 helper**

在 `src/views/direction/composables/useDirectionFetch.js` 第 42 行后新增：

```javascript
const resolveDefaultMonthlyPlan = (monthlyPlanList) => {
  const sorted = monthlyPlanList
    .map(mp => ({
      item: mp,
      month: getDateOnlyMonth(mp.month)
    }))
    .filter(({ month }) => month !== null)
    .sort((a, b) => a.month - b.month)

  if (sorted.length === 0) return null

  const currentMonth = new Date().getMonth() + 1
  const current = sorted.find(({ month }) => month === currentMonth)

  return current?.item || sorted[0].item
}
```

- [ ] **Step 2: 检查 helper 输入输出**

确认 helper 满足以下契约：

```javascript
resolveDefaultMonthlyPlan([]) // null
resolveDefaultMonthlyPlan([{ month: 'bad' }]) // null
resolveDefaultMonthlyPlan([{ id: 'mp1', month: '2026-01-01' }]) // mp1
```

---

### Task 2: 修改 `fetchData()` 初始月份选择

**Files:**
- Modify: `src/views/direction/composables/useDirectionFetch.js`

- [ ] **Step 1: 替换 `fetchData()` 中默认选第一个月的代码块**

将 `src/views/direction/composables/useDirectionFetch.js` 第 129-146 行：

```javascript
// 选中第一个目标的第一个月，并预加载该月 dailyPlans
if (selectedGoal.value && monthlyPlans.value.length > 0) {
  // 按月份排序，选中第一个月
  const sorted = [...monthlyPlans.value].sort((a, b) => {
    const mA = getDateOnlyMonth(a.month) || 0
    const mB = getDateOnlyMonth(b.month) || 0
    return mA - mB
  })
  const firstMp = sorted[0]
  if (firstMp) {
    const firstMonth = getDateOnlyMonth(firstMp.month)
    if (firstMonth) {
      selectedMonth.value = firstMonth
      // 预加载该月的 dailyPlans
      await loadDailyPlans(firstMp.id, { force: true })
    }
  }
}
```

替换为：

```javascript
// 当前月在目标范围内时优先展开当前月，否则展开排序后的第一个月
if (selectedGoal.value && monthlyPlans.value.length > 0) {
  const targetMp = resolveDefaultMonthlyPlan(monthlyPlans.value)
  const targetMonth = targetMp ? getDateOnlyMonth(targetMp.month) : null

  if (targetMp && targetMonth) {
    selectedMonth.value = targetMonth
    await loadDailyPlans(targetMp.id, { force: true })
  }
}
```

---

### Task 3: 修改 `selectedGoal` watch 目标切换月份选择

**Files:**
- Modify: `src/views/direction/composables/useDirectionFetch.js`

- [ ] **Step 1: 替换 `selectedGoal` watch 中默认选第一个月的代码块**

将 `src/views/direction/composables/useDirectionFetch.js` 第 191-206 行：

```javascript
// 选中第一个月并预加载
if (monthlyPlans.value.length > 0) {
  const sorted = [...monthlyPlans.value].sort((a, b) => {
    const mA = getDateOnlyMonth(a.month) || 0
    const mB = getDateOnlyMonth(b.month) || 0
    return mA - mB
  })
  const firstMp = sorted[0]
  if (firstMp) {
    const firstMonth = getDateOnlyMonth(firstMp.month)
    if (firstMonth) {
      selectedMonth.value = firstMonth
      await loadDailyPlans(firstMp.id, { force: true })
    }
  }
}
```

替换为：

```javascript
// 当前月在目标范围内时优先展开当前月，否则展开排序后的第一个月
if (monthlyPlans.value.length > 0) {
  const targetMp = resolveDefaultMonthlyPlan(monthlyPlans.value)
  const targetMonth = targetMp ? getDateOnlyMonth(targetMp.month) : null

  if (targetMp && targetMonth) {
    selectedMonth.value = targetMonth
    await loadDailyPlans(targetMp.id, { force: true })
  }
}
```

---

### Task 4: 补充 `fetchData()` 默认月份单元测试

**Files:**
- Modify: `src/views/direction/composables/__tests__/useDirectionFetch.spec.js`

- [ ] **Step 1: 导入 `selectedMonth`**

将文件顶部从 `useDirectionState` 的导入列表：

```javascript
  selectedGoal,
  showAddModal
```

改为：

```javascript
  selectedGoal,
  selectedMonth,
  showAddModal
```

- [ ] **Step 2: 在 `beforeEach()` 中重置 `selectedMonth`**

在 `selectedGoal.value = null` 后新增：

```javascript
  selectedMonth.value = null
```

- [ ] **Step 3: 增加当前月存在时的测试**

在 `describe('useDirectionFetch', () => {` 内新增测试：

```javascript
  it('selects the current month during initial fetch when the current month exists', async () => {
    const currentMonth = new Date().getMonth() + 1
    const currentMonthDate = `2026-${String(currentMonth).padStart(2, '0')}-01`
    const fallbackMonthDate = currentMonth === 1 ? '2026-02-01' : '2026-01-01'

    db.plans.list.mockResolvedValue([
      { id: 'p1', title: '目标 1' }
    ])
    db.monthlyPlans.list.mockResolvedValue([
      { id: 'mp-fallback', plan_id: 'p1', month: fallbackMonthDate },
      { id: 'mp-current', plan_id: 'p1', month: currentMonthDate }
    ])
    db.dailyPlans.list.mockResolvedValue([])

    const { fetchData } = useDirectionFetch()

    await fetchData()

    expect(selectedMonth.value).toBe(currentMonth)
    expect(db.dailyPlans.list).toHaveBeenCalledWith('mp-current')
  })
```

- [ ] **Step 4: 增加当前月不存在时的测试**

在同一 `describe` 内继续新增测试：

```javascript
  it('falls back to the first available month during initial fetch when the current month is missing', async () => {
    const currentMonth = new Date().getMonth() + 1
    const firstMonth = currentMonth === 3 ? 4 : 3
    const laterMonth = currentMonth === 7 ? 8 : 7
    const firstMonthDate = `2026-${String(firstMonth).padStart(2, '0')}-01`
    const laterMonthDate = `2026-${String(laterMonth).padStart(2, '0')}-01`

    db.plans.list.mockResolvedValue([
      { id: 'p1', title: '目标 1' }
    ])
    db.monthlyPlans.list.mockResolvedValue([
      { id: 'mp-later', plan_id: 'p1', month: laterMonthDate },
      { id: 'mp-first', plan_id: 'p1', month: firstMonthDate }
    ])
    db.dailyPlans.list.mockResolvedValue([])

    const { fetchData } = useDirectionFetch()

    await fetchData()

    expect(selectedMonth.value).toBe(firstMonth)
    expect(db.dailyPlans.list).toHaveBeenCalledWith('mp-first')
  })
```

---

### Task 5: 验证

**Files:**
- Test: `src/views/direction/composables/__tests__/useDirectionFetch.spec.js`
- Validate: `src/views/direction/composables/useDirectionFetch.js`

- [ ] **Step 1: 运行 Direction fetch 单测**

Run:

```bash
pnpm test:unit src/views/direction/composables/__tests__/useDirectionFetch.spec.js
```

Expected:

```text
PASS src/views/direction/composables/__tests__/useDirectionFetch.spec.js
```

- [ ] **Step 2: 运行生产构建**

Run:

```bash
pnpm build
```

Expected:

```text
✓ built in
```

- [ ] **Step 3: 人工验证 Direction 页面**

验证步骤：

1. 启动开发服务。
2. 打开 `/direction`。
3. 找到包含当前月的目标，确认中间列表默认展开当前月。
4. 找到不包含当前月的目标，确认中间列表默认展开该目标排序后的第一个月。
5. 切换左侧目标，确认默认月份规则与初始进入页面一致。
