# 全量代码质量修复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复代码审查发现的 20 个质量问题（8 HIGH / 9 MEDIUM / 3 LOW），涵盖安全漏洞、功能 bug、响应性丢失、资源泄漏等。

**Architecture:** 按依赖链优先策略分 6 阶段：底层基础 → Composables → Day 视图 → 其他视图 → 通用组件 → 验证。先修底层确保上层修复不因基础问题返工。

**Tech Stack:** Vue 3 Composition API, Pinia, Supabase, Vitest

---

## 阶段 1：底层基础

### Task 1: supabase.js — 环境变量校验 & Key 遮蔽

**Files:**
- Modify: `src/services/supabase.js:21-27`

- [ ] **Step 1: 修改环境变量缺失处理**

将 warn + 继续执行改为 throw 阻止启动，并遮蔽 key：

```js
// src/services/supabase.js, 替换第 21-27 行
if (!supabaseUrl || !supabaseAnonKey) {
  const maskedKey = supabaseAnonKey
    ? supabaseAnonKey.slice(0, 4) + '***'
    : undefined
  console.error('Supabase 环境变量缺失:', {
    url: supabaseUrl,
    key: maskedKey
  })
  throw new Error('Supabase 环境变量缺失，应用无法启动')
}
```

- [ ] **Step 2: 运行测试验证**

Run: `pnpm test:unit`
Expected: PASS

---

### Task 2: pageStateStore — 修复响应性丢失

**Files:**
- Modify: `src/stores/pageStateStore.js:29-32`
- Modify: `src/stores/__tests__/pageStateStore.spec.js`

- [ ] **Step 1: 移除展开运算符**

```js
// src/stores/pageStateStore.js, 替换 return 块
return {
  state,
  resetAll
}
```

- [ ] **Step 2: 更新测试文件**

测试中 `store.day` → `store.state.day`，`store.summary` → `store.state.summary`：

```js
// src/stores/__tests__/pageStateStore.spec.js
it('resets page-level transient states in one call', () => {
  const store = usePageStateStore()
  store.state.day.showAddDrawer = true
  store.state.summary.isCreating = true
  store.resetAll()

  expect(store.state.day.showAddDrawer).toBe(false)
  expect(store.state.summary.isCreating).toBe(false)
})

it('has correct initial state', () => {
  const store = usePageStateStore()
  expect(store.state.day.showAddDrawer).toBe(false)
  expect(store.state.day.editingTaskIndex).toBe(null)
  expect(store.state.summary.isCreating).toBe(false)
  expect(store.state.direction.showCategoryModal).toBe(false)
})
```

- [ ] **Step 3: 运行测试验证**

Run: `pnpm vitest run src/stores/__tests__/pageStateStore.spec.js`
Expected: PASS

---

### Task 3: pomodoroStore — 清理 setInterval

**Files:**
- Modify: `src/stores/pomodoroStore.js`

- [ ] **Step 1: 添加 stop 方法到返回对象**

`stopTimer` 已存在于 store 内部（第 108-113 行），只需在 return 中暴露为 `stop`：

```js
// src/stores/pomodoroStore.js, 替换 return 块（第 184-195 行）
return {
    activeTask,
    showModal,
    elapsedSeconds,
    formattedTime,
    setActiveTask,
    reset,
    closeModal,
    openModal,
    startTimer,
    stopTimer,
    stop: stopTimer
}
```

- [ ] **Step 2: 运行测试验证**

Run: `pnpm test:unit`
Expected: PASS

---

### Task 4: dayStore — useRoute() lazy 化

**Files:**
- Modify: `src/stores/dayStore.js:21`

- [ ] **Step 1: 将 useRoute() 移入使用处**

将第 21 行的 `const route = useRoute()` 移除，改为在 `routeDateContext` computed 内部调用：

```js
// src/stores/dayStore.js
// 删除第 21 行: const route = useRoute()
// 修改第 23 行 routeDateContext:
const routeDateContext = computed(() => {
  const route = useRoute()
  return getRouteDateContext(route.params, dateStore.currentDate)
})
```

- [ ] **Step 2: 运行测试验证**

Run: `pnpm test:unit`
Expected: PASS

---

## 阶段 2：Composables 层

### Task 5: useNotifications — 注释 & 重入保护

**Files:**
- Modify: `src/composables/useNotifications.js:36, 223, 283`

- [ ] **Step 1: 添加全局单例注释**

在第 36 行 `notifiedTaskIds` 声明处添加注释：

```js
// 全局单例：所有组件共享同一份通知历史，clearNotifiedHistory() 会清空全部
const notifiedTaskIds = ref(new Set())
```

- [ ] **Step 2: 添加 startListening 重入保护**

在 `startListening` 函数开头添加守卫：

```js
const startListening = (getScheduleItems) => {
    // 重入保护：已有轮询则跳过
    if (checkInterval) return

    stopListening()
    // ... 原有逻辑
```

- [ ] **Step 3: 添加 clearNotifiedHistory 注释**

```js
/**
 * 清除已通知历史记录
 * 注意：此操作影响全局，所有组件共享的通知历史都会被清空
 */
const clearNotifiedHistory = () => {
```

- [ ] **Step 4: 运行测试验证**

Run: `pnpm test:unit`
Expected: PASS

---

### Task 6: useResizable — 监听器去重

**Files:**
- Modify: `src/composables/useResizable.js:128-134`

- [ ] **Step 1: startResize 开头先移除旧监听器**

```js
// src/composables/useResizable.js, 替换 startResize 函数
const startResize = (e) => {
    // 先移除可能残留的旧监听器，防止快速 mousedown 叠加
    document.removeEventListener('mousemove', handleResize)
    document.removeEventListener('mouseup', stopResize)

    isResizing.value = true
    document.addEventListener('mousemove', handleResize)
    document.addEventListener('mouseup', stopResize)
    document.body.style.cursor = 'col-resize'
    document.body.style.userSelect = 'none'
}
```

- [ ] **Step 2: 运行测试验证**

Run: `pnpm test:unit`
Expected: PASS

---

### Task 7: useGlobalLoading — 导出 cleanup 函数

**Files:**
- Modify: `src/composables/useGlobalLoading.js`

- [ ] **Step 1: 添加 cleanup 函数**

在文件末尾 `export` 前添加：

```js
function cleanup() {
  clearTimeout(showTimer)
  clearTimeout(hideTimer)
  showTimer = null
  hideTimer = null
  pendingCount.value = 0
  visible.value = false
  NProgress.done()
}
```

- [ ] **Step 2: 在 export 中添加 cleanup**

```js
export {
  beginGlobalLoading,
  beginRouteLoading,
  endGlobalLoading,
  endRouteLoading,
  trackGlobalLoading,
  useGlobalLoading,
  cleanup
}
```

- [ ] **Step 3: 运行测试验证**

Run: `pnpm test:unit`
Expected: PASS

---

### Task 8: onboardingStore — activeStep 动态初始化

**Files:**
- Modify: `src/stores/onboardingStore.js:30`

- [ ] **Step 1: 从 completedSteps 计算 activeStep**

```js
// src/stores/onboardingStore.js, 替换第 30 行
const activeStep = ref(
  defaultSteps.find((step) => !completedSteps.value.includes(step)) || defaultSteps[defaultSteps.length - 1]
)
```

- [ ] **Step 2: 运行测试验证**

Run: `pnpm test:unit`
Expected: PASS

---

## 阶段 3：Day 视图

### Task 9: useDayModal — 数组索引改用 id

**Files:**
- Modify: `src/views/day/composables/useDayModal.js`

- [ ] **Step 1: editingTaskIndex 改为 editingTaskId**

```js
// src/views/day/composables/useDayModal.js — 完整替换
import { ref, computed } from 'vue'
import { useDayData } from './useDayData'

export function useDayModal() {
    const { dailySchedule } = useDayData()

    const showAddModal = ref(false)
    const editingTaskId = ref(null)

    const editingTask = computed(() => {
        if (editingTaskId.value !== null && dailySchedule.value) {
            return dailySchedule.value.find(t => t.id === editingTaskId.value) || null
        }
        return null
    })

    const openAddModal = () => {
        editingTaskId.value = null
        showAddModal.value = true
    }

    const openEditModal = (id) => {
        editingTaskId.value = id
        showAddModal.value = true
    }

    return {
        showAddModal,
        editingTaskId,
        editingTask,
        openAddModal,
        openEditModal
    }
}
```

- [ ] **Step 2: 更新测试文件**

Run: `pnpm vitest run src/views/day/composables/__tests__/useDayModal.spec.js`（如存在）
Expected: 如有测试需同步更新调用方

---

### Task 10: Sidebar.vue — 滑动完成 & edit-task 改用任务 id

**Files:**
- Modify: `src/views/day/components/Sidebar.vue`

- [ ] **Step 1: 修改 swipeToComplete 回调**

回调中用任务 id 查找对象，确保即使排序变化也能找到正确任务：

```js
// src/views/day/components/Sidebar.vue, 替换 swipeToComplete 回调
const { swipeState, getOffset: getSwipeOffset, handleTouchStart: swipeStart, handleTouchMove: swipeMove, handleTouchEnd: swipeEnd } = useSwipeToComplete(async (activeId) => {
  if (activeId === null) return
  const item = dailySchedule.value.find(t => t.id === activeId)
  if (item) await handleToggleComplete(item)
})

const handleTouchStart = (e, item) => swipeStart(e, item.id)
const handleTouchMove = (e, item) => swipeMove(e, item.id)
const handleTouchEnd = (e, item) => swipeEnd(item.id)
```

- [ ] **Step 2: 修改 edit-task emit 改用任务 id**

模板中所有 `$emit('edit-task', section.item._originalIndex)` 改为 `$emit('edit-task', section.item.id)`，所有 `item._originalIndex` 同理。共约 8 处。

同步修改模板中滑动相关绑定：`handleTouchStart($event, section.item._originalIndex)` → `handleTouchStart($event, section.item)`，`getSwipeOffset(section.item._originalIndex)` → `getSwipeOffset(section.item.id)` 等。

- [ ] **Step 3: 运行测试验证**

Run: `pnpm test:unit`
Expected: PASS

---

### Task 11: TaskItemWrapper.vue — 监听器去重

**Files:**
- Modify: `src/views/day/components/TaskItemWrapper.vue:96-104`

- [ ] **Step 1: 添加监听器绑定标志**

```js
// src/views/day/components/TaskItemWrapper.vue
// 在 script setup 中添加标志变量（约第 63 行后）
let listenersBound = false

function bindDocumentSession() {
  if (listenersBound) return
  listenersBound = true
  document.addEventListener('mousemove', handleMouseMove)
  document.addEventListener('mouseup', handleMouseUp)
}

function onDragStart(event) {
  session.startDrag(event)
  bindDocumentSession()
}

// 修改 handleMouseUp 重置标志
function handleMouseUp() {
  document.removeEventListener('mousemove', handleMouseMove)
  document.removeEventListener('mouseup', handleMouseUp)
  listenersBound = false
  session.finish()
}
```

- [ ] **Step 2: 运行测试验证**

Run: `pnpm test:unit`
Expected: PASS

---

### Task 12: PomodoroTimerModal.vue — 圆环 circumference 修正

**Files:**
- Modify: `src/views/day/components/PomodoroTimerModal.vue:42`

- [ ] **Step 1: 将硬编码 290 改为计算值**

```vue
<!-- 第 42 行，替换 :stroke-dasharray="290" -->
:stroke-dasharray="2 * Math.PI * 46"
```

- [ ] **Step 2: 运行测试验证**

Run: `pnpm test:unit`
Expected: PASS

---

### Task 13: useAddEventForm — 时间格式校验

**Files:**
- Modify: `src/views/day/composables/useAddEventForm.js:132`

- [ ] **Step 1: 在 submit 中添加时间格式校验**

```js
// src/views/day/composables/useAddEventForm.js, 在第 132 行前添加校验
const submit = withLoadingLock(async () => {
    if (!isValid.value) {
        markAllTouched()
        return
    }

    isSubmitting.value = true
    const timeParts = form.time.split(':')
    const hours = parseInt(timeParts[0], 10)
    const minutes = parseInt(timeParts[1], 10)
    if (isNaN(hours) || isNaN(minutes) || hours < 0 || hours > 23 || minutes < 0 || minutes > 59) {
        error('时间格式无效')
        isSubmitting.value = false
        return
    }
    const durationValue = parseFloat(form.duration)
    // ... 原有逻辑
```

- [ ] **Step 2: 运行测试验证**

Run: `pnpm test:unit`
Expected: PASS

---

### Task 14: Day 视图未使用 import 清理

**Files:**
- Modify: `src/views/day/components/TaskItem.vue:151`
- Modify: `src/views/day/components/PomodoroTimerModal.vue:106`
- Modify: `src/views/day/components/MobileAddEventDrawer.vue:166`

- [ ] **Step 1: TaskItem.vue — 删除 CheckCircle**

```js
// src/views/day/components/TaskItem.vue 第 151 行
// 原: import { Play, CheckCircle, Timer, Maximize2 } from 'lucide-vue-next'
import { Play, Timer, Maximize2 } from 'lucide-vue-next'
```

- [ ] **Step 2: PomodoroTimerModal.vue — 删除 Badge**

```js
// src/views/day/components/PomodoroTimerModal.vue 第 106 行
// 删除: import { Badge } from '@/components/ui/badge'
```

- [ ] **Step 3: MobileAddEventDrawer.vue — 删除 formatDuration**

```js
// src/views/day/components/MobileAddEventDrawer.vue 第 166 行
// 删除: import { formatDuration } from '@/utils/formatDuration'
```

- [ ] **Step 4: 运行测试验证**

Run: `pnpm test:unit`
Expected: PASS

---

## 阶段 4：其他视图

### Task 15: HabitMobileListSheet — isTodayCompleted 修复

**Files:**
- Modify: `src/views/habits/HabitMobileListSheet.vue:226-231`

- [ ] **Step 1: 改用日期数字比较**

```js
// src/views/habits/HabitMobileListSheet.vue, 替换 isTodayCompleted
const isTodayCompleted = (habit) => {
  if (!habit.completedDays || !Array.isArray(habit.completedDays)) return false
  return habit.completedDays.includes(new Date().getDate())
}
```

- [ ] **Step 2: 运行测试验证**

Run: `pnpm test:unit`
Expected: PASS

---

### Task 16: 频率表单逻辑去重

**Files:**
- Create: `src/views/habits/composables/useHabitFrequencyForm.js`
- Modify: `src/views/habits/components/AddHabitModal.vue`
- Modify: `src/views/habits/components/EditHabitModal.vue`

- [ ] **Step 1: 创建 useHabitFrequencyForm composable**

```js
// src/views/habits/composables/useHabitFrequencyForm.js
import { computed } from 'vue'
import {
  createDefaultHabitFrequency,
  normalizeHabitFrequency
} from '@/views/habits/utils/habitFrequency'

export const WEEKDAY_OPTIONS = [
  { label: '一', value: 1 },
  { label: '二', value: 2 },
  { label: '三', value: 3 },
  { label: '四', value: 4 },
  { label: '五', value: 5 },
  { label: '六', value: 6 },
  { label: '日', value: 7 }
]

export const MONTH_DAY_OPTIONS = Array.from({ length: 31 }, (_, index) => index + 1)

export function useHabitFrequencyForm(form) {
  const canSubmitFrequency = computed(() => {
    if (form.frequencyType === 'weekly') return form.weekdays.length > 0
    if (form.frequencyType === 'monthly') return form.monthDays.length > 0
    return true
  })

  const toggleWeekday = (weekday) => {
    form.weekdays = form.weekdays.includes(weekday)
      ? form.weekdays.filter((item) => item !== weekday)
      : [...form.weekdays, weekday].sort((a, b) => a - b)
  }

  const toggleMonthDay = (day) => {
    form.monthDays = form.monthDays.includes(day)
      ? form.monthDays.filter((item) => item !== day)
      : [...form.monthDays, day].sort((a, b) => a - b)
  }

  const buildFrequencyPayload = () => {
    if (form.frequencyType === 'weekly') {
      return normalizeHabitFrequency({
        type: 'weekly',
        weekdays: form.weekdays
      })
    }
    if (form.frequencyType === 'monthly') {
      return normalizeHabitFrequency({
        type: 'monthly',
        monthDays: form.monthDays
      })
    }
    return createDefaultHabitFrequency()
  }

  return {
    WEEKDAY_OPTIONS,
    MONTH_DAY_OPTIONS,
    canSubmitFrequency,
    toggleWeekday,
    toggleMonthDay,
    buildFrequencyPayload
  }
}
```

- [ ] **Step 2: 更新 AddHabitModal.vue**

删除重复的常量和函数定义，改为 import composable：

```js
// src/views/habits/components/AddHabitModal.vue
// 删除第 157-220 行（WEEKDAY_OPTIONS, MONTH_DAY_OPTIONS, canSubmitFrequency, toggleWeekday, toggleMonthDay, buildFrequencyPayload）
// 添加 import:
import { useHabitFrequencyForm } from '@/views/habits/composables/useHabitFrequencyForm'

// 在 form 定义之后添加:
const {
  WEEKDAY_OPTIONS,
  MONTH_DAY_OPTIONS,
  canSubmitFrequency,
  toggleWeekday,
  toggleMonthDay,
  buildFrequencyPayload
} = useHabitFrequencyForm(form)
```

- [ ] **Step 3: 更新 EditHabitModal.vue**

同上，删除重复定义，改为 import composable：

```js
// src/views/habits/components/EditHabitModal.vue
// 删除第 205-275 行（WEEKDAY_OPTIONS, MONTH_DAY_OPTIONS, canSubmitFrequency, toggleWeekday, toggleMonthDay, buildFrequencyPayload）
// 添加 import:
import { useHabitFrequencyForm } from '@/views/habits/composables/useHabitFrequencyForm'

// 在 form 定义之后添加:
const {
  WEEKDAY_OPTIONS,
  MONTH_DAY_OPTIONS,
  canSubmitFrequency,
  toggleWeekday,
  toggleMonthDay,
  buildFrequencyPayload
} = useHabitFrequencyForm(form)
```

- [ ] **Step 4: 运行测试验证**

Run: `pnpm test:unit`
Expected: PASS

---

### Task 17: DirectionSidebar — 删除未使用变量

**Files:**
- Modify: `src/views/direction/components/DirectionSidebar.vue:105-107`

- [ ] **Step 1: 删除 monthPlan 变量**

```js
// src/views/direction/components/DirectionSidebar.vue, 替换 systemLoad computed
const systemLoad = computed(() => {
  if (!selectedGoal.value || !selectedMonth.value) return 0

  const goalId = selectedGoal.value.goal_id
  const month = selectedMonth.value

  return getDirectionMonthlyProgress({
    dailyTasks,
    goalId,
    month,
    isGoalDayCompleted
  })
})
```

- [ ] **Step 2: 运行测试验证**

Run: `pnpm test:unit`
Expected: PASS

---

### Task 18: MissionBoardMonthHeader — 月份补零修复

**Files:**
- Modify: `src/views/direction/components/MissionBoardMonthHeader.vue:4`

- [ ] **Step 1: 条件补零**

```vue
<!-- 第 4 行，替换 0{{ month }}月 -->
{{ month < 10 ? '0' + month : month }}月
```

- [ ] **Step 2: 运行测试验证**

Run: `pnpm test:unit`
Expected: PASS

---

## 阶段 5：通用组件 & 路由

### Task 19: Navbar — navItems 响应化

**Files:**
- Modify: `src/components/Navbar.vue:228-238`

注：原始审查发现的 isActive 导航高亮问题经复核为误报（/month 和 /year 属于时序体系的子路由，激活"时序"是正确行为），跳过。

- [ ] **Step 1: navItems 改为 computed**

`getTodayPath()` 在模块加载时求值，跨午夜后过期。改为 computed：

```js
// src/components/Navbar.vue
// 删除第 228-230 行的 getTodayPath 函数
// 将第 233-238 行的 navItems 改为 computed
const navItems = computed(() => [
  { name: '时序 ', path: buildDayPath(new Date()), icon: LayoutGrid, base: '/day' },
  { name: '习惯', path: '/habits', icon: CheckCircle2, base: '/habits' },
  { name: '所向 ', path: '/direction', icon: Compass, base: '/direction' },
  { name: '总结', path: '/summary', icon: BookOpen, base: '/summary' },
])
```

模板中 `v-for` 已支持 computed，无需额外修改模板。

- [ ] **Step 3: 运行测试验证**

Run: `pnpm test:unit`
Expected: PASS

---

### Task 20: DurationPicker — 单位自动切换扩展

**Files:**
- Modify: `src/components/ui/DurationPicker.vue:153-158`

- [ ] **Step 1: 扩展 modelValue watcher**

```js
// src/components/ui/DurationPicker.vue, 替换第 153-158 行
watch(() => props.modelValue, (newVal, oldVal) => {
  if (switchingUnit) return
  // 初始加载或值跨越阈值时自动切换单位
  if (oldVal === undefined || oldVal === 0) {
    durationUnit.value = newVal > 1 ? 'hour' : 'minute'
  } else if (newVal > 1 && durationUnit.value === 'minute') {
    durationUnit.value = 'hour'
  } else if (newVal <= 1 && durationUnit.value === 'hour') {
    durationUnit.value = 'minute'
  }
})
```

- [ ] **Step 2: 运行测试验证**

Run: `pnpm test:unit`
Expected: PASS

---

### Task 21: TimePicker — 输入校验

**Files:**
- Modify: `src/components/ui/TimePicker.vue:11`

- [ ] **Step 1: 添加输入校验**

在 Input 的 `@update:model-value` 中添加格式校验，仅允许合法时间格式透传：

```vue
<!-- src/components/ui/TimePicker.vue, 第 11 行 -->
<Input
  :id="id"
  :name="id"
  :model-value="displayValue"
  @update:model-value="handleInput"
  :placeholder="placeholder"
  class="h-9 font-mono pr-8"
  @click="openTimePopover = true"
  @input="openTimePopover = true"
  @keydown.down.prevent="openTimePopover = true"
  @keyup.enter="$emit('submit')"
/>
```

在 script 中添加 handleInput 函数：

```js
const handleInput = (val) => {
  if (!val) {
    emit('update:modelValue', '')
    return
  }
  // 仅允许 HH:mm 格式
  if (/^\d{0,2}:?\d{0,2}$/.test(val) && val.length <= 5) {
    emit('update:modelValue', val)
  }
}
```

- [ ] **Step 2: 运行测试验证**

Run: `pnpm test:unit`
Expected: PASS

---

### Task 22: 其他视图未使用 import 清理

**Files:**
- Modify: `src/views/summary/index.vue:138`
- Modify: `src/views/habits/components/EditHabitModal.vue:173`

- [ ] **Step 1: summary/index.vue — 删除 PageIntroBanner**

```js
// src/views/summary/index.vue 第 138 行
// 删除: import PageIntroBanner from '@/components/PageIntroBanner.vue'
```

- [ ] **Step 2: EditHabitModal.vue — 删除 DialogDescription**

```js
// src/views/habits/components/EditHabitModal.vue 第 173 行
// 原: import { Dialog, DialogContent, DialogTitle, DialogDescription } from '@/components/ui/dialog'
import { Dialog, DialogContent, DialogTitle } from '@/components/ui/dialog'
```

- [ ] **Step 3: 运行测试验证**

Run: `pnpm test:unit`
Expected: PASS

---

## 阶段 6：验证

### Task 23: 全量验证

- [ ] **Step 1: 运行全部测试**

Run: `pnpm test:unit`
Expected: ALL PASS

- [ ] **Step 2: 运行生产构建**

Run: `pnpm build`
Expected: 构建成功，无报错

- [ ] **Step 3: 启动开发服务器手动验证**

Run: `pnpm dev`

验证点：
1. 习惯页面今日完成状态是否正确显示（绿色勾选）
2. 番茄钟定时器切换页面后是否正确停止
3. 导航栏在各路由下高亮是否正确
