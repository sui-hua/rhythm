# 目标未完成只读延后查询 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 Direction 目标增加“未完成延后展示”配置，并让 Day 页面按目标级窗口只读补查历史未完成 `daily_plans`，同时保留原始计划日期。

**Architecture:** 方案分成四段推进。先把 `carry_over_lookback_days` 接到 `plans` 的读写链路和目标弹窗，再扩展 `dailyPlans` 的 Day 查询入口，让它在不改写 `daily_plans.day` 的前提下返回当天项和历史未完成项，随后把“历史未完成”标记沉到 `dailySchedule` 执行项模型，最后在 Sidebar 和 Timeline 两条展示链路都显示原始计划日期。

**Tech Stack:** Vue 3 Composition API, Vitest, Supabase `createBase` query wrapper, Tailwind CSS 4

---

## 实施约束

- 写代码时优先补充有解释价值的注释，重点覆盖 Day 查询拼装、carry-over 窗口过滤、执行项标记和 UI 原始日期提示这些不直观的逻辑
- 注释只解释关键判断和数据语义，不堆砌无信息量描述

---

## 文件结构

```text
database/
└── current-structure.md                         # 修改：补充 plans 新字段说明

src/
├── services/
│   └── db/
│       ├── plans.js                            # 修改：plans 读写支持 carry_over_lookback_days
│       └── dailyPlans.js                       # 修改：新增 Day 页补查入口
├── views/
│   ├── direction/
│   │   ├── components/
│   │   │   └── AddGoalModal.vue               # 修改：目标弹窗增加配置控件
│   │   └── composables/
│   │       ├── useDirectionGoals.js           # 修改：创建、更新、编辑回填 carry_over_lookback_days
│   │       └── __tests__/
│   │           └── useDirectionGoals.spec.js  # 修改：覆盖目标配置保存与回填
│   └── day/
│       ├── components/
│       │   ├── Sidebar.vue                    # 修改：列表显示历史未完成原始日期
│       │   └── TaskItem.vue                   # 修改：时间轴卡片显示历史未完成原始日期
│       └── composables/
│           ├── useDayData.js                  # 修改：改走 Day 页补查入口
│           ├── useDayExecutionItems.js        # 修改：标注 carry-over 元数据
│           └── __tests__/
│               ├── useDayData.spec.js         # 修改：覆盖只读补查查询与完成行为
│               └── useDayExecutionItems.spec.js # 新建：覆盖 carry-over 执行项标记
```

---

## Task 1: 接入目标级配置

**Files:**
- Modify: `src/views/direction/composables/__tests__/useDirectionGoals.spec.js`
- Modify: `src/services/db/plans.js`
- Modify: `src/views/direction/composables/useDirectionGoals.js`
- Modify: `src/views/direction/components/AddGoalModal.vue`
- Modify: `database/current-structure.md`

- [ ] **Step 1: 正式执行时先通过 Supabase MCP 给 `plans` 加字段**

执行方式：使用 Supabase MCP 对真实数据库执行 DDL，而不是只改本地文档。

SQL:

```sql
alter table public.plans
add column if not exists carry_over_lookback_days integer not null default 0;
```

Expected: `public.plans` 新增 `carry_over_lookback_days`，旧记录自动按默认值 `0` 处理

- [ ] **Step 2: 先写 Direction 侧的失败测试，锁定创建与更新都要保存 `carry_over_lookback_days`**

在 [useDirectionGoals.spec.js](/D:/司占锋/demo/rhythm/src/views/direction/composables/__tests__/useDirectionGoals.spec.js) 追加这两个用例：

```javascript
  it('writes carry-over lookback days into created plans when adding a goal', async () => {
    const { handleAddGoal } = useDirectionGoals()

    await handleAddGoal({
      title: '读书',
      description: '',
      startMonth: 4,
      endMonth: 4,
      category_id: null,
      task_time: '07:30',
      duration: 45,
      carry_over_lookback_days: 3
    })

    expect(db.plans.create).toHaveBeenCalledWith(expect.objectContaining({
      carry_over_lookback_days: 3
    }))
  })

  it('writes carry-over lookback days into updated plans when editing a goal', async () => {
    editingGoal.value = {
      plan_id: 'p1',
      title: '目标',
      carry_over_lookback_days: 0
    }

    const { handleUpdateGoal } = useDirectionGoals()

    await handleUpdateGoal({
      title: '目标',
      category_id: null,
      task_time: '09:00',
      duration: 30,
      carry_over_lookback_days: 5
    })

    expect(db.plans.update).toHaveBeenCalledWith('p1', expect.objectContaining({
      carry_over_lookback_days: 5
    }))
  })
```

- [ ] **Step 3: 跑定向测试，确认它先失败**

Run: `pnpm test:unit -- src/views/direction/composables/__tests__/useDirectionGoals.spec.js`

Expected: 新增的断言失败，提示 `carry_over_lookback_days` 尚未写入 `db.plans.create` 或 `db.plans.update`

- [ ] **Step 4: 在 plans 服务、目标弹窗和保存链路里接上这个字段**

在 [plans.js](/D:/司占锋/demo/rhythm/src/services/db/plans.js) 的注释与读写说明中补上字段；在 [useDirectionGoals.js](/D:/司占锋/demo/rhythm/src/views/direction/composables/useDirectionGoals.js) 创建/更新 payload 时带上它；在 [AddGoalModal.vue](/D:/司占锋/demo/rhythm/src/views/direction/components/AddGoalModal.vue) 增加“未完成延后展示”开关和天数输入，并在打开编辑弹窗时正确回填。

计划使用下面这组核心改动：

```javascript
// src/views/direction/composables/useDirectionGoals.js
const planData = {
  user_id: authStore.userId,
  title: newGoal.title,
  description: newGoal.description || '',
  year: `${year}-01-01`,
  category_id: newGoal.category_id || null,
  task_time: newGoal.task_time || '09:00',
  duration: newGoal.duration || 30,
  carry_over_lookback_days: newGoal.carry_over_lookback_days || 0,
  status: 'active',
  priority: 2,
}

if (newCarryOverLookbackDays !== undefined) {
  updates.carry_over_lookback_days = newCarryOverLookbackDays
}
```

```vue
<!-- src/views/direction/components/AddGoalModal.vue -->
<div class="form-group gap-3">
  <div class="flex items-center justify-between">
    <Label class="form-label">未完成延后展示</Label>
    <Checkbox
      :checked="form.carryOverEnabled"
      @update:checked="(checked) => form.carryOverEnabled = Boolean(checked)"
      class="h-5 w-5 rounded-md"
    />
  </div>

  <div v-if="form.carryOverEnabled" class="grid gap-2">
    <Label for="goal-carry-over-days" class="form-label">延后展示天数</Label>
    <Input
      id="goal-carry-over-days"
      v-model.number="form.carryOverLookbackDays"
      type="number"
      min="1"
      step="1"
      class="form-input"
    />
    <p class="text-xs text-muted-foreground">
      开启后，Day 页面会额外显示该目标过去若干天未完成的日计划，但不会修改原计划日期。
    </p>
  </div>
</div>
```

```javascript
// src/views/direction/components/AddGoalModal.vue
const payload = {
  title: form.title,
  startMonth: parseInt(form.startMonth),
  endMonth: parseInt(form.endMonth),
  category_id: form.category_id === 'none' ? null : form.category_id,
  task_time: form.task_time,
  duration: Math.round(form.duration * 60),
  carry_over_lookback_days: form.carryOverEnabled
    ? Math.max(1, Number(form.carryOverLookbackDays || 1))
    : 0,
  status: 'active',
}
```

- [ ] **Step 5: 更新数据库结构快照文档**

在 [current-structure.md](/D:/司占锋/demo/rhythm/database/current-structure.md) 的 `public.plans` 字段列表里补一行：

```md
- `carry_over_lookback_days`
```

并在说明区补一句：

```md
- `carry_over_lookback_days` 用于 Day 页面只读补查历史未完成目标日计划，`0` 表示关闭
```

- [ ] **Step 6: 再跑一次 Direction 定向测试**

Run: `pnpm test:unit -- src/views/direction/composables/__tests__/useDirectionGoals.spec.js`

Expected: PASS，新增用例与原有用例全部通过

---

## Task 2: 扩展 Day 查询入口，支持按目标级窗口补查历史未完成

**Files:**
- Modify: `src/services/db/dailyPlans.js`
- Modify: `src/views/day/composables/__tests__/useDayData.spec.js`
- Modify: `src/views/day/composables/useDayData.js`

- [ ] **Step 1: 先写 Day 数据层的失败测试，锁定查询入口切换**

在 [useDayData.spec.js](/D:/司占锋/demo/rhythm/src/views/day/composables/__tests__/useDayData.spec.js) 调整 mock，并追加用例验证 `fetchTasks()` 走新的补查入口：

```javascript
vi.mock('@/services/database', () => ({
  db: {
    tasks: {
      list: vi.fn(),
      update: vi.fn()
    },
    dailyPlans: {
      listForDayView: vi.fn(),
      update: vi.fn()
    },
    habits: {
      listLite: vi.fn(),
      listLogsByDate: vi.fn(),
      log: vi.fn(),
      deleteLog: vi.fn()
    }
  }
}))
```

```javascript
  it('fetches daily plans through the day-view carry-over query', async () => {
    db.dailyPlans.listForDayView.mockResolvedValue([
      { id: 'plan-today', day: '2026-04-28', status: 0 },
      { id: 'plan-old', day: '2026-04-27', status: 0 }
    ])

    const { fetchTasks, dailySchedule } = useDayData()
    await fetchTasks({ showLoading: false })

    expect(db.dailyPlans.listForDayView).toHaveBeenCalledTimes(1)
    expect(db.dailyPlans.listForDayView).toHaveBeenCalledWith(expect.any(Date))
    expect(dailySchedule.value.filter(item => item.type === 'daily_plan')).toHaveLength(2)
  })
```

- [ ] **Step 2: 跑定向测试，确认 `listForDayView` 还不存在**

Run: `pnpm test:unit -- src/views/day/composables/__tests__/useDayData.spec.js`

Expected: FAIL，错误指向 `db.dailyPlans.listForDayView` 未被调用或 `fetchTasks()` 仍在调用 `listByDate`

- [ ] **Step 3: 在 dailyPlans 服务里新增 `listForDayView(date)`，用两段查询实现“只读延后”**

在 [dailyPlans.js](/D:/司占锋/demo/rhythm/src/services/db/dailyPlans.js) 保留现有 `listByDate()`，新增专供 Day 页面使用的入口。核心实现按下面的结构写：

```javascript
const plansBase = client.createBase('plans')

const toDateOnly = (date) => {
  const year = date.getFullYear()
  const month = String(date.getMonth() + 1).padStart(2, '0')
  const day = String(date.getDate()).padStart(2, '0')
  return `${year}-${month}-${day}`
}

const subtractDays = (date, days) => {
  const next = new Date(date)
  next.setDate(next.getDate() - days)
  return next
}

async function getMaxCarryOverLookbackDays() {
  const rows = await plansBase.query(q => q
    .select('carry_over_lookback_days')
    .gt('carry_over_lookback_days', 0)
    .order('carry_over_lookback_days', { ascending: false })
    .limit(1)
  )

  return Number(rows?.[0]?.carry_over_lookback_days || 0)
}
```

```javascript
async listForDayView(date) {
  const targetDateStr = toDateOnly(date)
  const maxLookbackDays = await getMaxCarryOverLookbackDays()

  if (maxLookbackDays === 0) {
    return await this.listByDate(date)
  }

  const earliestDateStr = toDateOnly(subtractDays(date, maxLookbackDays))

  const rows = await supabase.query(q => q
    .select(`
      *,
      monthly_plans (
        id,
        task_time,
        duration,
        plans (
          id,
          task_time,
          duration,
          carry_over_lookback_days
        )
      )
    `)
    .gte('day', earliestDateStr)
    .lte('day', targetDateStr)
    .order('day', { ascending: true })
  )

  return (rows || []).filter((plan) => {
    if (plan.day === targetDateStr) return true
    if (plan.status !== 0) return false

    const lookbackDays = Number(plan.monthly_plans?.plans?.carry_over_lookback_days || 0)
    if (lookbackDays <= 0) return false

    const planDate = new Date(`${plan.day}T00:00:00`)
    const earliestAllowed = subtractDays(date, lookbackDays)
    return planDate >= earliestAllowed && plan.day < targetDateStr
  })
}
```

- [ ] **Step 4: 把 Day 数据层切到新的服务入口**

在 [useDayData.js](/D:/司占锋/demo/rhythm/src/views/day/composables/useDayData.js) 把下面这一行：

```javascript
db.dailyPlans.listByDate(startOfDay),
```

改成：

```javascript
db.dailyPlans.listForDayView(startOfDay),
```

其他三类数据获取逻辑保持不动。

- [ ] **Step 5: 再跑一次 Day 数据层定向测试**

Run: `pnpm test:unit -- src/views/day/composables/__tests__/useDayData.spec.js`

Expected: PASS，原有“切换完成状态”测试继续通过，新增“走补查入口”测试也通过

---

## Task 3: 在执行项模型层标注历史未完成状态与原始日期

**Files:**
- Create: `src/views/day/composables/__tests__/useDayExecutionItems.spec.js`
- Modify: `src/views/day/composables/useDayExecutionItems.js`
- Modify: `src/views/day/composables/useDayData.js`

- [ ] **Step 1: 先写执行项模型的失败测试，锁定 carry-over 元数据**

新建 [useDayExecutionItems.spec.js](/D:/司占锋/demo/rhythm/src/views/day/composables/__tests__/useDayExecutionItems.spec.js)：

```javascript
import { describe, expect, it } from 'vitest'
import { buildDayExecutionItems } from '@/views/day/composables/useDayExecutionItems'

describe('buildDayExecutionItems', () => {
  it('marks historical pending daily plans as carry-over items', () => {
    const items = buildDayExecutionItems({
      targetDate: new Date('2026-04-28T00:00:00'),
      dailyPlans: [
        {
          id: 'plan-old',
          title: '补做复盘',
          day: '2026-04-27',
          task_time: '08:00',
          duration: 30,
          status: 0
        }
      ]
    })

    expect(items[0]).toMatchObject({
      id: 'plan-old',
      type: 'daily_plan',
      isCarryOver: true,
      carryOverSourceDate: '2026-04-27',
      carryOverLabel: '原计划 4月27日'
    })
  })
})
```

- [ ] **Step 2: 跑定向测试，确认新的字段当前还不存在**

Run: `pnpm test:unit -- src/views/day/composables/__tests__/useDayExecutionItems.spec.js`

Expected: FAIL，断言指出 `isCarryOver` 或 `carryOverLabel` 未生成

- [ ] **Step 3: 扩展执行项构建函数，给历史未完成日计划打上展示元数据**

在 [useDayExecutionItems.js](/D:/司占锋/demo/rhythm/src/views/day/composables/useDayExecutionItems.js) 调整函数签名，并在 `daily_plan` 分支里补这组字段：

```javascript
const formatCarryOverLabel = (dateOnly) => {
  const [year, month, day] = dateOnly.split('-').map(Number)
  return `原计划 ${month}月${day}日`
}

export function buildDayExecutionItems({
  targetDate = null,
  tasks = [],
  dailyPlans = [],
  habits = [],
  habitLogs = []
}) {
  const targetDateStr = targetDate
    ? `${targetDate.getFullYear()}-${String(targetDate.getMonth() + 1).padStart(2, '0')}-${String(targetDate.getDate()).padStart(2, '0')}`
    : null

  // ...

  const isCarryOver = Boolean(
    targetDateStr &&
    plan.day &&
    plan.day < targetDateStr &&
    !isDailyPlanCompleted(plan.status)
  )

  schedule.push({
    id: plan.id,
    sourceLabel: 'daily_plan',
    type: 'daily_plan',
    original: plan,
    startHour: startHourVal,
    durationHours,
    rawDuration: durationHours,
    time: startTimeStr,
    duration: durationStr,
    category: '今日计划',
    title: plan.title,
    description: plan.description || '',
    completed: isDailyPlanCompleted(plan.status),
    isCarryOver,
    carryOverSourceDate: isCarryOver ? plan.day : null,
    carryOverLabel: isCarryOver ? formatCarryOverLabel(plan.day) : ''
  })
}
```

- [ ] **Step 4: 把目标日期传入执行项构建函数**

在 [useDayData.js](/D:/司占锋/demo/rhythm/src/views/day/composables/useDayData.js) 的 `dailySchedule` 计算属性里，把调用改成：

```javascript
return buildDayExecutionItems({
  targetDate: new Date(
    routeDateContext.value.year,
    routeDateContext.value.month - 1,
    routeDateContext.value.day
  ),
  tasks: tasks.value,
  dailyPlans: dailyPlans.value,
  habits: habits.value,
  habitLogs: habitLogs.value
})
```

- [ ] **Step 5: 再跑执行项与 Day 数据层测试**

Run: `pnpm test:unit -- src/views/day/composables/__tests__/useDayExecutionItems.spec.js src/views/day/composables/__tests__/useDayData.spec.js`

Expected: PASS，执行项模型能产出 carry-over 元数据，Day 数据层的既有行为不回退

---

## Task 4: 在 Sidebar 和 Timeline 两条展示链路显示原始计划日期

**Files:**
- Modify: `src/views/day/components/Sidebar.vue`
- Modify: `src/views/day/components/TaskItem.vue`

- [ ] **Step 1: 先在 Sidebar 列表项里显示历史未完成原始日期**

在 [Sidebar.vue](/D:/司占锋/demo/rhythm/src/views/day/components/Sidebar.vue) 的标题块后面加一行，仅在 `item.isCarryOver` 时显示：

```vue
<p
  v-if="item.isCarryOver"
  class="text-[11px] font-medium text-amber-600 dark:text-amber-400 truncate"
>
  {{ item.carryOverLabel }}
</p>
```

把包裹标题的容器保持成：

```vue
<div class="flex-1 min-w-0 flex flex-col gap-0.5">
```

这样不会打乱现有桌面端和移动端列表结构。

- [ ] **Step 2: 在 Timeline 卡片里显示相同标记**

在 [TaskItem.vue](/D:/司占锋/demo/rhythm/src/views/day/components/TaskItem.vue) 里，对中长任务都补一个轻量标签：

```vue
<p
  v-if="task.isCarryOver"
  class="text-[10px] font-bold uppercase tracking-wider text-amber-600 dark:text-amber-400 ml-4"
>
  {{ task.carryOverLabel }}
</p>
```

插入位置：

- 中等卡片：标题行之后
- 长卡片：分类/时间行之后、主标题之前

不要改变卡片定位、时长计算或并排布局算法。

- [ ] **Step 3: 跑与 Day 视图最贴近的测试，再做一次手工核验**

Run: `pnpm test:unit -- src/views/day/composables/__tests__/useDayExecutionItems.spec.js src/views/day/composables/__tests__/useDayData.spec.js src/views/direction/composables/__tests__/useDirectionGoals.spec.js`

Expected: PASS，三个定向测试文件全部通过

手工核验：

1. `pnpm dev`
2. 打开 `/direction`，创建一个开启“未完成延后展示”的目标，填入 `3`
3. 给该目标创建一条昨天的 `daily_plan`，保持未完成
4. 打开今天的 `/day/:year/:month/:day`
5. 确认该项被展示出来，并且 UI 上写着 `原计划 x月x日`
6. 勾选完成后刷新页面，确认它消失，且数据库原 `day` 不变

---

## 实施检查清单

- [ ] `plans` 的创建、更新、编辑回填都支持 `carry_over_lookback_days`
- [ ] `carry_over_lookback_days = 0` 时，Day 页面不补查历史未完成
- [ ] `carry_over_lookback_days > 0` 时，Day 页面只读补查窗口内的历史未完成
- [ ] 历史未完成项完成后只更新原记录 `status`
- [ ] Sidebar 和 Timeline 都能显示原始计划日期
- [ ] `database/current-structure.md` 已同步新字段说明
