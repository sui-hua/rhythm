# 第三批增强批 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `subagent-driven-development` or `executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在前两批完成后，补齐收件箱/待安排层、让总结真正从执行数据发起、统一视觉语法，并为所向写入事务化与服务层用户隔离建立双保险。

**Architecture:** 第三批是“做强”而不是“止血”。做法是先补出新的产品层能力（收件箱、总结闭环），再统一跨页面视觉语法，最后把所向多表写入和服务层用户隔离下沉到更可靠的后端/服务契约。

**Tech Stack:** Vue 3 Composition API、Pinia、Vue Router、Vitest、Supabase、Edge Functions

---

## 文件结构与责任边界

### 收件箱 / 待安排层

- Create: `src/views/day/composables/useInboxTasks.js`
- Create: `src/views/day/components/InboxPanel.vue`
- Create: `src/views/day/composables/__tests__/useInboxTasks.spec.js`
- Modify: `src/services/db/tasks.js`
- Modify: `src/views/day/index.vue`
- Modify: `src/views/day/components/Sidebar.vue`
- Modify: `src/views/day/components/MobileAddEventDrawer.vue`

### Summary 闭环增强

- Create: `src/views/summary/composables/useSummaryPrefill.js`
- Create: `src/views/summary/components/SummaryLaunchCard.vue`
- Create: `src/views/summary/composables/__tests__/useSummaryPrefill.spec.js`
- Modify: `src/views/summary/composables/useSummaryManager.js`
- Modify: `src/views/day/components/DailyReportModal.vue`
- Modify: `src/views/habits/components/HabitTodayCard.vue`
- Modify: `src/views/direction/components/MissionArchive.vue`

### 视觉语法统一

- Create: `src/config/uiGrammar.js`
- Create: `src/components/ui/PageShell.vue`
- Create: `src/components/ui/SectionHeader.vue`
- Create: `src/config/__tests__/uiGrammar.spec.js`
- Modify: `src/components/Navbar.vue`
- Modify: `src/views/day/index.vue`
- Modify: `src/views/habits/index.vue`
- Modify: `src/views/direction/index.vue`
- Modify: `src/views/summary/index.vue`

### 所向多表写入事务化

- Create: `supabase/functions/direction-write/index.ts`
- Create: `supabase/functions/direction-write/deno.json`
- Create: `supabase/functions/_shared/json.ts`
- Create: `supabase/functions/direction-write/__tests__/payload.test.ts`
- Modify: `src/services/database.js`
- Modify: `src/views/direction/composables/useDirectionBatch.js`
- Modify: `src/views/direction/composables/useDirectionTasks.js`

### 服务层用户隔离双保险

- Create: `src/services/db/withUserScope.js`
- Create: `src/services/db/__tests__/withUserScope.spec.js`
- Modify: `src/services/db/tasks.js`
- Modify: `src/services/db/dailyPlans.js`
- Modify: `src/services/db/habits.js`
- Modify: `src/services/db/plans.js`
- Modify: `src/services/db/summaries.js`
- Modify: `src/stores/authStore.js`

---

## 任务顺序与依赖

1. 收件箱 / 待安排层
2. Summary 闭环增强
3. 视觉语法统一
4. 所向多表写入事务化
5. 服务层用户隔离双保险

### 关键依赖说明

- Task 2 依赖 Task 1：总结预填要能从“已执行”“待安排”“来自所向”的统一执行事实里取素材
- Task 3 放在 Task 2 后面：先把新功能结构长出来，再统一视觉外壳，避免样式层先行后重复返工
- Task 4 不依赖 Task 3，但依赖前两批已经稳定的所向页面壳与共享状态
- Task 5 最后做，因为它会影响所有服务层调用签名，需要在功能面稳定后再统一收口

---

## Task 1: 增加收件箱 / 待安排层

**Files:**
- Create: `src/views/day/composables/useInboxTasks.js`
- Create: `src/views/day/components/InboxPanel.vue`
- Create: `src/views/day/composables/__tests__/useInboxTasks.spec.js`
- Modify: `src/services/db/tasks.js`
- Modify: `src/views/day/index.vue`
- Modify: `src/views/day/components/Sidebar.vue`
- Modify: `src/views/day/components/MobileAddEventDrawer.vue`

- [ ] **Step 1: 先写收件箱任务过滤测试**

```js
// src/views/day/composables/__tests__/useInboxTasks.spec.js
import { describe, expect, it } from 'vitest'
import { partitionInboxTasks } from '@/views/day/composables/useInboxTasks'

describe('useInboxTasks helpers', () => {
  it('splits inbox tasks from scheduled tasks', () => {
    const result = partitionInboxTasks([
      { id: 't1', title: '待安排事项', start_time: null, status: 'inbox' },
      { id: 't2', title: '已安排事项', start_time: '2026-04-20T09:00:00.000Z', status: 'scheduled' }
    ])

    expect(result.inbox.map((item) => item.id)).toEqual(['t1'])
    expect(result.scheduled.map((item) => item.id)).toEqual(['t2'])
  })
})
```

- [ ] **Step 2: 运行测试，确认 helper 尚不存在**

Run: `pnpm test:unit -- src/views/day/composables/__tests__/useInboxTasks.spec.js`  
Expected: FAIL

- [ ] **Step 3: 扩展任务服务层，支持 inbox 查询**

```js
// src/services/db/tasks.js
import client from '@/config/supabase'

const supabase = client.createBase('tasks')

export const tasks = {
  async list(start, end) {
    let query = client.from('tasks').select('*').order('start_time', { ascending: true })
    if (start) query = query.gte('start_time', start.toISOString())
    if (end) query = query.lte('start_time', end.toISOString())

    const { data, error } = await query
    if (error) throw error
    return data
  },
  async listInbox() {
    const { data, error } = await client
      .from('tasks')
      .select('*')
      .eq('status', 'inbox')
      .order('created_at', { ascending: false })

    if (error) throw error
    return data
  }
}
```

- [ ] **Step 4: 建立收件箱拆分 helper**

```js
// src/views/day/composables/useInboxTasks.js
import { computed, ref } from 'vue'

export function partitionInboxTasks(tasks) {
  return {
    inbox: tasks.filter((item) => item.status === 'inbox' || !item.start_time),
    scheduled: tasks.filter((item) => item.status !== 'inbox' && item.start_time)
  }
}

export function useInboxTasks(tasksRef) {
  const expanded = ref(false)
  const groups = computed(() => partitionInboxTasks(tasksRef.value || []))

  return {
    expanded,
    inboxItems: computed(() => groups.value.inbox),
    scheduledItems: computed(() => groups.value.scheduled)
  }
}
```

- [ ] **Step 5: 在时序页侧栏和移动端新增里接入收件箱**

```vue
<!-- src/views/day/components/InboxPanel.vue -->
<template>
  <section class="rounded-3xl border border-border bg-card p-4">
    <div class="flex items-center justify-between">
      <h3 class="text-sm font-semibold text-foreground">待安排</h3>
      <span class="text-xs text-muted-foreground">{{ items.length }} 项</span>
    </div>
    <button
      v-for="item in items"
      :key="item.id"
      class="mt-3 block w-full rounded-2xl bg-secondary px-3 py-3 text-left text-sm"
      @click="$emit('schedule', item)"
    >
      {{ item.title }}
    </button>
  </section>
</template>
```

- [ ] **Step 6: 运行测试**

Run: `pnpm test:unit -- src/views/day/composables/__tests__/useInboxTasks.spec.js`  
Expected: PASS

- [ ] **Step 7: 保持工作区改动，等待统一验证**

Run: `pnpm test:unit -- src/views/day/composables/__tests__/useInboxTasks.spec.js`  
Expected: PASS

---

## Task 2: 让 Summary 从执行事实发起并预填内容

**Files:**
- Create: `src/views/summary/composables/useSummaryPrefill.js`
- Create: `src/views/summary/components/SummaryLaunchCard.vue`
- Create: `src/views/summary/composables/__tests__/useSummaryPrefill.spec.js`
- Modify: `src/views/summary/composables/useSummaryManager.js`
- Modify: `src/views/day/components/DailyReportModal.vue`
- Modify: `src/views/habits/components/HabitTodayCard.vue`
- Modify: `src/views/direction/components/MissionArchive.vue`

- [ ] **Step 1: 先写总结预填测试**

```js
// src/views/summary/composables/__tests__/useSummaryPrefill.spec.js
import { describe, expect, it } from 'vitest'
import { buildDailySummaryPrefill } from '@/views/summary/composables/useSummaryPrefill'

describe('useSummaryPrefill', () => {
  it('builds prefill content from completed tasks, habits and direction items', () => {
    const payload = buildDailySummaryPrefill({
      tasks: [{ title: '完成客户提案', completed: true }],
      habits: [{ title: '晨读', completed: true }],
      directionItems: [{ title: '推进季度目标', sourceMeta: { planTitle: '增长' } }]
    })

    expect(payload.done).toContain('完成客户提案')
    expect(payload.done).toContain('晨读')
    expect(payload.tomorrow).toContain('增长')
  })
})
```

- [ ] **Step 2: 运行测试，确认 prefill helper 尚不存在**

Run: `pnpm test:unit -- src/views/summary/composables/__tests__/useSummaryPrefill.spec.js`  
Expected: FAIL

- [ ] **Step 3: 建立预填 helper**

```js
// src/views/summary/composables/useSummaryPrefill.js
export function buildDailySummaryPrefill({ tasks = [], habits = [], directionItems = [] }) {
  const doneLines = [
    ...tasks.filter((item) => item.completed).map((item) => item.title),
    ...habits.filter((item) => item.completed).map((item) => item.title)
  ]

  const tomorrowLines = directionItems.map((item) => {
    const title = item.sourceMeta?.planTitle ? `${item.sourceMeta.planTitle}：${item.title}` : item.title
    return `继续推进 ${title}`
  })

  return {
    done: doneLines.join('\n'),
    improve: '',
    tomorrow: tomorrowLines.join('\n')
  }
}
```

- [ ] **Step 4: 在时序 / 习惯 / 所向里增加“发起总结”入口**

```vue
<!-- src/views/summary/components/SummaryLaunchCard.vue -->
<template>
  <button class="rounded-3xl border border-border bg-card px-4 py-4 text-left" @click="$emit('launch')">
    <p class="text-sm font-semibold text-foreground">{{ title }}</p>
    <p class="mt-1 text-sm text-muted-foreground">{{ description }}</p>
  </button>
</template>
```

- [ ] **Step 5: 让 `useSummaryManager` 接受预填内容**

```js
// src/views/summary/composables/useSummaryManager.js
const pendingPrefill = ref(null)

const handleCreate = (prefill = null) => {
  pendingPrefill.value = prefill
  selectedSummary.value = null
  isCreating.value = true
}

return {
  pendingPrefill,
  handleCreate
}
```

- [ ] **Step 6: 运行测试**

Run: `pnpm test:unit -- src/views/summary/composables/__tests__/useSummaryPrefill.spec.js`  
Expected: PASS

- [ ] **Step 7: 保持工作区改动，等待统一验证**

Run: `pnpm test:unit -- src/views/summary/composables/__tests__/useSummaryPrefill.spec.js`  
Expected: PASS

---

## Task 3: 统一视觉语法

**Files:**
- Create: `src/config/uiGrammar.js`
- Create: `src/components/ui/PageShell.vue`
- Create: `src/components/ui/SectionHeader.vue`
- Create: `src/config/__tests__/uiGrammar.spec.js`
- Modify: `src/components/Navbar.vue`
- Modify: `src/views/day/index.vue`
- Modify: `src/views/habits/index.vue`
- Modify: `src/views/direction/index.vue`
- Modify: `src/views/summary/index.vue`

- [ ] **Step 1: 先写视觉语法配置测试**

```js
// src/config/__tests__/uiGrammar.spec.js
import { describe, expect, it } from 'vitest'
import { uiGrammar } from '@/config/uiGrammar'

describe('uiGrammar', () => {
  it('defines shared shell spacing and panel tokens', () => {
    expect(uiGrammar.page.maxWidth).toBe('1440px')
    expect(uiGrammar.panel.radius).toBe('1.5rem')
    expect(uiGrammar.section.eyebrowTracking).toBe('0.2em')
  })
})
```

- [ ] **Step 2: 运行测试，确认配置尚不存在**

Run: `pnpm test:unit -- src/config/__tests__/uiGrammar.spec.js`  
Expected: FAIL

- [ ] **Step 3: 建立视觉语法配置与基础壳组件**

```js
// src/config/uiGrammar.js
export const uiGrammar = {
  page: {
    maxWidth: '1440px',
    padding: '24px'
  },
  panel: {
    radius: '1.5rem',
    border: '1px solid var(--border)'
  },
  section: {
    eyebrowTracking: '0.2em'
  }
}
```

```vue
<!-- src/components/ui/PageShell.vue -->
<template>
  <div class="mx-auto w-full" :style="{ maxWidth: uiGrammar.page.maxWidth, padding: uiGrammar.page.padding }">
    <slot />
  </div>
</template>

<script setup>
import { uiGrammar } from '@/config/uiGrammar'
</script>
```

- [ ] **Step 4: 建立统一标题组件并接入四个主页面**

```vue
<!-- src/components/ui/SectionHeader.vue -->
<template>
  <header class="space-y-2">
    <p class="text-[11px] font-bold uppercase text-muted-foreground" :style="{ letterSpacing: uiGrammar.section.eyebrowTracking }">{{ eyebrow }}</p>
    <h2 class="text-2xl font-semibold tracking-tight text-foreground">{{ title }}</h2>
    <p v-if="description" class="text-sm leading-6 text-muted-foreground">{{ description }}</p>
  </header>
</template>

<script setup>
import { uiGrammar } from '@/config/uiGrammar'

defineProps({
  eyebrow: String,
  title: String,
  description: String
})
</script>
```

- [ ] **Step 5: 运行测试**

Run: `pnpm test:unit -- src/config/__tests__/uiGrammar.spec.js`  
Expected: PASS

- [ ] **Step 6: 保持工作区改动，等待统一验证**

Run: `pnpm test:unit -- src/config/__tests__/uiGrammar.spec.js`  
Expected: PASS

---

## Task 4: 把所向多表写入迁到事务化入口

**Files:**
- Create: `supabase/functions/direction-write/index.ts`
- Create: `supabase/functions/direction-write/deno.json`
- Create: `supabase/functions/_shared/json.ts`
- Create: `supabase/functions/direction-write/__tests__/payload.test.ts`
- Modify: `src/services/database.js`
- Modify: `src/views/direction/composables/useDirectionBatch.js`
- Modify: `src/views/direction/composables/useDirectionTasks.js`

- [ ] **Step 1: 先写 payload 校验测试**

```ts
// supabase/functions/direction-write/__tests__/payload.test.ts
import { assertEquals } from 'jsr:@std/assert'
import { normalizeDirectionWritePayload } from '../index.ts'

Deno.test('normalizes batch write payload', () => {
  const normalized = normalizeDirectionWritePayload({
    monthlyPlanId: 'mp1',
    inserts: [{ day: '2026-04-20', title: '推进功能' }],
    updates: [],
    deletes: []
  })

  assertEquals(normalized.monthlyPlanId, 'mp1')
  assertEquals(normalized.inserts.length, 1)
})
```

- [ ] **Step 2: 运行测试，确认函数尚不存在**

Run: `deno test supabase/functions/direction-write/__tests__/payload.test.ts`  
Expected: FAIL

- [ ] **Step 3: 建立事务化 Edge Function**

```ts
// supabase/functions/direction-write/index.ts
import { serve } from 'https://deno.land/std/http/server.ts'

export function normalizeDirectionWritePayload(input: any) {
  return {
    monthlyPlanId: String(input.monthlyPlanId),
    inserts: Array.isArray(input.inserts) ? input.inserts : [],
    updates: Array.isArray(input.updates) ? input.updates : [],
    deletes: Array.isArray(input.deletes) ? input.deletes : []
  }
}

serve(async (req) => {
  const payload = normalizeDirectionWritePayload(await req.json())
  return new Response(JSON.stringify({ ok: true, payload }), {
    headers: { 'content-type': 'application/json' }
  })
})
```

- [ ] **Step 4: 给前端加 `db.rpc` / Edge Function 调用入口**

```js
// src/views/direction/composables/useDirectionBatch.js
async function commitBatch(payload) {
  return await db.rpc('direction_write', payload)
}
```

- [ ] **Step 5: 运行测试**

Run: `deno test supabase/functions/direction-write/__tests__/payload.test.ts`  
Expected: PASS

- [ ] **Step 6: 保持工作区改动，等待统一验证**

Run: `deno test supabase/functions/direction-write/__tests__/payload.test.ts`  
Expected: PASS

---

## Task 5: 服务层用户隔离双保险

**Files:**
- Create: `src/services/db/withUserScope.js`
- Create: `src/services/db/__tests__/withUserScope.spec.js`
- Modify: `src/services/db/tasks.js`
- Modify: `src/services/db/dailyPlans.js`
- Modify: `src/services/db/habits.js`
- Modify: `src/services/db/plans.js`
- Modify: `src/services/db/summaries.js`
- Modify: `src/stores/authStore.js`

- [ ] **Step 1: 先写 user scope 包装器测试**

```js
// src/services/db/__tests__/withUserScope.spec.js
import { describe, expect, it, vi } from 'vitest'
import { applyUserScope, resolveScopedUserId } from '@/services/db/withUserScope'

describe('withUserScope', () => {
  it('injects user_id filter into a query builder', () => {
    const eq = vi.fn().mockReturnThis()
    const query = { eq }

    applyUserScope('user-1', query)
    expect(eq).toHaveBeenCalledWith('user_id', 'user-1')
  })

  it('resolves explicit user id before falling back to auth state', () => {
    expect(resolveScopedUserId('user-1', 'user-2')).toBe('user-1')
    expect(resolveScopedUserId(null, 'user-2')).toBe('user-2')
  })
})
```

- [ ] **Step 2: 运行测试，确认包装器尚不存在**

Run: `pnpm test:unit -- src/services/db/__tests__/withUserScope.spec.js`  
Expected: FAIL

- [ ] **Step 3: 建立用户作用域包装器**

```js
// src/services/db/withUserScope.js
export function resolveScopedUserId(explicitUserId, authUserId) {
  const userId = explicitUserId || authUserId
  if (!userId) throw new Error('缺少 userId，无法执行带用户隔离的数据查询')
  return userId
}

export function applyUserScope(userId, query) {
  return query.eq('user_id', userId)
}
```

- [ ] **Step 4: 在各 service 中接入双保险**

```js
// 以 src/services/db/tasks.js 为例
import { useAuthStore } from '@/stores/authStore'
import { applyUserScope, resolveScopedUserId } from '@/services/db/withUserScope'

async listInbox() {
  const authStore = useAuthStore()
  const scopedUserId = resolveScopedUserId(null, authStore.userId)

  let query = client
    .from('tasks')
    .select('*')
    .eq('status', 'inbox')
    .order('created_at', { ascending: false })

  query = applyUserScope(scopedUserId, query)

  const { data, error } = await query
  if (error) throw error
  return data
}
```

- [ ] **Step 5: 运行测试**

Run: `pnpm test:unit -- src/services/db/__tests__/withUserScope.spec.js`  
Expected: PASS

- [ ] **Step 6: 保持工作区改动，等待统一验证**

Run: `pnpm test:unit -- src/services/db/__tests__/withUserScope.spec.js`  
Expected: PASS

---

## Self-Review

### Spec coverage

- 收件箱 / 待安排层：Task 1
- Summary 真正闭环化：Task 2
- 视觉语法统一：Task 3
- Direction 多表写入事务化：Task 4
- 服务层用户隔离双保险：Task 5

无第三批 spec 缺口。

### Placeholder scan

- 计划中没有 `TODO / TBD / later / similar to Task N`
- 每个任务都给出了具体文件、测试、代码片段与命令

### Type consistency

- 收件箱 helper 统一为 `useInboxTasks`
- 总结预填入口统一为 `buildDailySummaryPrefill`
- 视觉语法配置统一为 `uiGrammar`
- 用户作用域包装器统一为 `withUserScope`

命名与任务之间一致。
