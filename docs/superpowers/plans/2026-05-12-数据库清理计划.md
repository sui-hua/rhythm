# 数据库清理实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 清理废弃表、冗余列、错误约束和不一致的兼容层

**Architecture:** 分两层推进：先通过 MCP SQL 迁移修改数据库结构，再逐一清理代码中的兼容逻辑和废弃字段。数据库变更和代码变更互相独立，可并行。

**Tech Stack:** Supabase MCP (apply_migration)、Vue 3、Pinia

---

### Task 1: SQL 数据库变更

通过 Supabase MCP 对数据库执行结构变更。

**注意：** 迁移时必须备份 `summaries` 和 `habits` 表数据。

- [ ] **Step 1: 备份数据**

```sql
-- 执行前备份
CREATE TABLE IF NOT EXISTS backup_summaries_20260512 AS SELECT * FROM summaries;
CREATE TABLE IF NOT EXISTS backup_habits_20260512 AS SELECT * FROM habits;
```

- [ ] **Step 2: 删除 `summaries.mood` 列**

```sql
ALTER TABLE summaries DROP COLUMN IF EXISTS mood;
```

- [ ] **Step 3: 删除 `summaries.scope` 的 NOT NULL 约束和 check 约束**

```sql
ALTER TABLE summaries ALTER COLUMN scope DROP NOT NULL;
ALTER TABLE summaries DROP CONSTRAINT IF EXISTS summaries_scope_check;
```

- [ ] **Step 4: 修正已有数据的 scope 值**

```sql
UPDATE summaries SET scope = NULL WHERE scope IS NOT NULL;
```

- [ ] **Step 5: 删除 `habits.archived` 冗余列**

```sql
ALTER TABLE habits DROP COLUMN IF EXISTS archived;
```

- [ ] **Step 6: 删除废弃的 `daily_summaries` 表**

```sql
DROP TABLE IF EXISTS daily_summaries CASCADE;
```

---

### Task 2: 清理 `summaries.js` scope 兼容层

**文件：** `src/services/db/summaries.js`

删除 `bridgeLegacyScope()`、`normalizeScope()`、`kindToLegacyScope`、`allowedScopes` 整段逻辑，简化 `persistSummary` 直接传递 payload。

- [ ] **Step 1: 删除兼容常量和函数**

删除以下内容（第 18-75 行）：
- `allowedScopes` 集合
- `kindToLegacyScope` 映射表
- `normalizeScope()` 函数
- `bridgeLegacyScope()` 函数

- [ ] **Step 2: 简化 `persistSummary`**

将：
```js
const persistSummary = async (payload) => {
  const bridgedPayload = bridgeLegacyScope(payload)
  const query = payload.id
    ? client.from(table).update(bridgedPayload).eq('id', payload.id)
    : client.from(table).insert(bridgedPayload)
```

改为：
```js
const persistSummary = async (payload) => {
  const query = payload.id
    ? client.from(table).update(payload).eq('id', payload.id)
    : client.from(table).insert(payload)
```

- [ ] **Step 3: 更新 `summaries.js` 的 JSDoc**

移除 `@param {string} [payload.scope]` 相关注释。

---

### Task 3: 清理 `summaryAdapters.js` mood 相关代码

**文件：** `src/views/summary/utils/summaryAdapters.js`

- [ ] **Step 1: 删除 `normalizeMood` 函数**（第 62-77 行）

- [ ] **Step 2: 更新 `resolveFormData`**

从返回值中移除 `mood`：

```js
const resolveFormData = (summary, kind) => {
  const content = normalizeContent(summary.content)

  if (kind === 'daily') {
    return {
      done: summary.done ?? content.done ?? '',
      improve: summary.improve ?? content.improve ?? '',
      tomorrow: summary.tomorrow ?? content.tomorrow ?? ''
    }
  }

  return {
    title: summary.title ?? '',
    text: summary.text ?? content.text ?? ''
  }
}
```

- [ ] **Step 3: 更新 `mapSummaryRowToRecord`**

移除 `mood: normalizeMood(row.mood)` 行：

```js
export const mapSummaryRowToRecord = (row) => ({
  id: row.id,
  user_id: row.user_id,
  kind: row.kind,
  period_start: row.period_start,
  period_end: row.period_end,
  title: row.title ?? null,
  content: normalizeContent(row.content),
  created_at: row.created_at,
  updated_at: row.updated_at ?? row.created_at
})
```

- [ ] **Step 4: 更新 `buildSummaryPayload`**

移除 `mood: normalizeMood(formData.mood)` 和第 284 行的 `.mood` 赋值：

```js
export const buildSummaryPayload = ({ kind, userId, period, formData, existingRecord }) => {
  const isDaily = kind === 'daily'

  return {
    ...(existingRecord?.id ? { id: existingRecord.id } : {}),
    user_id: userId,
    kind,
    period_start: period.periodStart,
    period_end: period.periodEnd,
    title: isDaily ? null : (formData.title?.trim() || null),
    content: isDaily
      ? {
          done: formData.done || '',
          improve: formData.improve || '',
          tomorrow: formData.tomorrow || ''
        }
      : {
          text: formData.text || ''
        }
  }
}
```

- [ ] **Step 5: 更新 `toUnifiedSummaryPayload`**

移除 `mood: normalizeMood(summary.mood)` 行：

```js
export const toUnifiedSummaryPayload = (summary) => {
  if (isUnifiedPayload(summary)) {
    return {
      ...summary,
      content: normalizeContent(summary.content)
    }
  }

  const kind = resolveKind(summary)
  const period = resolvePeriod(summary, kind)
  const formData = resolveFormData(summary, kind)

  return buildSummaryPayload({
    kind,
    userId: summary.user_id ?? summary.userId,
    period,
    formData,
    existingRecord: summary.id ? { id: summary.id } : null
  })
}
```

- [ ] **Step 6: 同步更新 JSDoc**

移除 `summaryAdapters.js` 中所有引用 `mood` 的注释块（第 7 行、第 48-77 行、第 157 行、第 188 行、第 202/215 行、第 253/262 行、第 307/315 行）。

---

### Task 4: 日总结表单删除 mood

- [ ] **Step 1: 清理 `useDailySummaryForm.js`**

**文件：** `src/views/summary/composables/useDailySummaryForm.js`

从 `formData` 初始值和 `watch` 分支中移除 mood：

```js
const formData = ref({
  done: '',
  improve: '',
  tomorrow: ''
})

watch(
  initialDataRef,
  (newVal) => {
    if (newVal) {
      const content = newVal.content && typeof newVal.content === 'object' && !Array.isArray(newVal.content)
        ? newVal.content
        : {}

      formData.value = {
        done: content.done ?? '',
        improve: content.improve ?? '',
        tomorrow: content.tomorrow ?? ''
      }
    } else {
      formData.value = { done: '', improve: '', tomorrow: '' }
    }
  },
  { immediate: true }
)
```

`buildPayload` 不变（已只含 done/improve/tomorrow），移除 mood 相关的 JSDoc。

- [ ] **Step 2: 清理 `DailySummaryForm.vue`**

**文件：** `src/views/summary/components/DailySummaryForm.vue`

删除心情输入框部分（第 30-40 行）：

```html
    <!-- 完整删除这段 -->
    <div class="grid gap-2">
      <label class="text-sm font-medium leading-none">今日心情</label>
      <Input
        v-model.number="formData.mood"
        type="number"
        min="1"
        max="5"
        placeholder="1 到 5"
        class="h-10"
      />
    </div>
```

同时移除 `Input` 组件引入（如果不再被引用）：

```js
// 确认是否还有其他 Input 使用，若无则删除
import { Input } from '@/components/ui/input'
```

同步更新 JSDoc 注释（第 70 行 mood 相关）。

---

### Task 5: 通用总结表单删除 mood

- [ ] **Step 1: 清理 `useGenericSummaryForm.js`**

**文件：** `src/views/summary/composables/useGenericSummaryForm.js`

移除 `mood` ref、对应的 watch 逻辑、返回值：

```js
const content = ref('')

// 删除整个 mood 相关的 ref

watch(
  initialDataRef,
  (newVal) => {
    if (newVal) {
      const summaryContent = newVal.content && typeof newVal.content === 'object' && !Array.isArray(newVal.content)
        ? newVal.content
        : {}
      title.value = newVal.title ?? ''
      content.value = summaryContent.text ?? (typeof newVal.content === 'string' ? newVal.content : '')
    } else {
      title.value = ''
      content.value = ''
    }
  },
  { immediate: true }
)

const buildPayload = () => ({
  title: title.value.trim(),
  text: content.value
})

// 返回中去掉 mood
return {
  title,
  content,
  typeName,
  placeholderText,
  buildPayload
}
```

同步更新 JSDoc（第 21 行、第 24 行、第 88 行 mood 类型注释）。

- [ ] **Step 2: 清理 `GenericSummaryForm.vue`**

**文件：** `src/views/summary/components/GenericSummaryForm.vue`

删除心情输入框部分（第 25-37 行）：

```html
    <!-- 完整删除这段 -->
    <div class="grid gap-2">
      <label class="text-sm font-medium leading-none">
        {{ typeName }}心情
      </label>
      <Input
        v-model.number="mood"
        type="number"
        min="1"
        max="5"
        placeholder="1 到 5"
        class="h-10"
      />
    </div>
```

更新 `script` 中的解构（第 109 行）：

```js
const { title, content, typeName, placeholderText, buildPayload } = useGenericSummaryForm(
```

移除 `mood` 导出变量。同样移除 `Input` 组件引用（若其它地方无使用）。

同步更新 JSDoc（第 69 行）。

- [ ] **Step 3: 清理 `typeName` 死代码分支**

在 `useGenericSummaryForm.js` 中，`typeName` 的 `'day'` 分支已不会被执行（GenericSummaryForm 只在非 day 类型渲染），删除它：

```js
const typeName = computed(() => {
  if (typeRef.value === 'week') return '周'
  if (typeRef.value === 'month') return '月'
  return '年'
})
```

---

### Task 6: 删除未使用的 `SummaryLaunchCard.vue`

**文件：** `src/views/summary/components/SummaryLaunchCard.vue`（删除）

- [ ] **Step 1: 确认无引用**

```bash
grep -r "SummaryLaunchCard" src/ --include="*.vue" --include="*.js"
```
应无结果。

- [ ] **Step 2: 删除文件**

删除 `src/views/summary/components/SummaryLaunchCard.vue`。

---

### Task 7: 更新测试

- [ ] **Step 1: 更新 `summaryAdapters.spec.js`**

**文件：** `src/views/summary/composables/__tests__/summaryAdapters.spec.js`

1. 从所有测试数据中移除 `mood` 字段
2. 删除 `'normalizes invalid mood values to null'` 测试用例（第 75-103 行）
3. 更新 `buildSummaryPayload` 的期望值，去掉 `mood: 5`

示例：daily payload 测试（第 106-128 行）改为：

```js
it('builds a daily payload from the daily form fields', () => {
  const payload = buildSummaryPayload({
    kind: 'daily',
    userId: 'u1',
    period: {
      periodStart: '2026-04-18T00:00:00.000Z',
      periodEnd: '2026-04-18T23:59:59.000Z'
    },
    formData: {
      done: '完成任务',
      improve: '少刷手机',
      tomorrow: '继续推进'
    }
  })

  expect(payload.title).toBeNull()
  expect(payload.content).toEqual({
    done: '完成任务',
    improve: '少刷手机',
    tomorrow: '继续推进'
  })
})
```

- [ ] **Step 2: 更新 `summaries.spec.js`**

**文件：** `src/services/db/__tests__/summaries.spec.js`

1. 将 `scope: 'day'` 改为不带 scope 的正常数据
2. 将 `scope: 'week'` 断言改为期望 payload 中不含 scope

```js
it('saves a summary without legacy scope bridging', async () => {
  const { summaries } = await import('@/services/db/summaries')

  await summaries.save({
    kind: 'daily',
    period_start: '2026-04-18T00:00:00.000Z',
    period_end: '2026-04-18T23:59:59.000Z',
    content: {
      done: '完成任务'
    },
    user_id: 'u1'
  })

  expect(insert).toHaveBeenCalledTimes(1)
  expect(insert).toHaveBeenCalledWith(expect.not.objectContaining({
    scope: expect.anything()
  }))
})
```

---

### Task 8: 运行测试验证

- [ ] **Step 1: 运行总结模块相关测试**

```bash
pnpm vue-tsc && vitest run src/views/summary/composables/__tests__/ src/services/db/__tests__/summaries.spec.js
```

Expected: 所有测试通过。

- [ ] **Step 2: 运行全量测试**

```bash
pnpm vue-tsc && vitest run
```

Expected: 全量测试通过，无意外失败。

---

## 验证清单

- [ ] `summaries` 表已无 `mood` 列
- [ ] `summaries.scope` 已无 NOT NULL + check 约束，已有数据为 null
- [ ] `habits` 表已无 `archived` 列
- [ ] `daily_summaries` 表已不存在
- [ ] 新建日总结保存成功，数据库中无 mood 值
- [ ] 习惯列表/归档功能正常
- [ ] 已有总结加载正常
