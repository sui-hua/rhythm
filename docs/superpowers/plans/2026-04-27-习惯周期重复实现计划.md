# 习惯周期重复实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 `habit` 增加“每日 / 每周固定星期几 / 每月固定几号”重复规则，并让 `day` 页只展示当天应执行的习惯。

**Architecture:** 本次实现继续复用 `habits.frequency` 作为唯一重复规则模型，不新增独立规则表，也不引入第三方重复规则库。重复规则解析、标准化和“某日期是否命中该习惯”判断统一收敛到 `src/views/habits/utils/habitFrequency.js`，由新增/编辑弹窗负责写入，由 `day` 页数据层负责读取并过滤。

**Tech Stack:** Vue 3、Vite、Pinia、Supabase、Vitest、项目现有 UI 组件（Dialog、Button、Input、TimePicker、DurationPicker）

---

## 文件结构

### 新增文件

- `src/views/habits/utils/habitFrequency.js`
  - 负责标准化 `frequency`
  - 提供默认频率构造
  - 提供星期映射
  - 提供“某日期是否命中习惯规则”的纯函数
- `src/views/habits/utils/__tests__/habitFrequency.spec.js`
  - 覆盖 `daily / weekly / monthly / 旧数据兼容 / 非法数据回退`

### 修改文件

- `src/views/habits/components/AddHabitModal.vue`
  - 新增重复方式 UI
  - 新增表单状态
  - 创建时写入 `frequency`
- `src/views/habits/components/EditHabitModal.vue`
  - 新增重复方式 UI
  - 从 `habitData.frequency` 回填
  - 保存时写入 `frequency`
- `src/views/day/composables/useDayData.js`
  - day 页 habits 读取后按当天日期与 `frequency` 做过滤
- `src/services/db/habits.js`
  - 补充 `frequency` 数据结构注释，保持 CRUD 透传认知一致
- `database/current-structure.md`
  - 补充 `habits.frequency` 当前运行时约定，避免后续误解

## 任务拆解

### Task 1：实现习惯重复规则工具函数

**Files:**
- Create: `src/views/habits/utils/habitFrequency.js`
- Test: `src/views/habits/utils/__tests__/habitFrequency.spec.js`

- [ ] **Step 1: 先写频率工具的失败测试**

在 `src/views/habits/utils/__tests__/habitFrequency.spec.js` 新增以下测试：

```js
import { describe, expect, it } from 'vitest'
import {
  createDefaultHabitFrequency,
  getWeekdayNumber,
  matchesHabitFrequency,
  normalizeHabitFrequency
} from '@/views/habits/utils/habitFrequency'

describe('habitFrequency', () => {
  it('returns daily as the default frequency', () => {
    expect(createDefaultHabitFrequency()).toEqual({ type: 'daily' })
  })

  it('maps JavaScript Sunday to weekday 7', () => {
    expect(getWeekdayNumber(new Date('2026-04-19T12:00:00.000Z'))).toBe(7)
  })

  it('keeps a valid weekly frequency', () => {
    expect(normalizeHabitFrequency({
      type: 'weekly',
      weekdays: [1, 3, 5]
    })).toEqual({
      type: 'weekly',
      weekdays: [1, 3, 5]
    })
  })

  it('falls back to daily when weekly weekdays become empty after filtering', () => {
    expect(normalizeHabitFrequency({
      type: 'weekly',
      weekdays: [0, 9, 'x']
    })).toEqual({ type: 'daily' })
  })

  it('matches a weekly habit on the configured weekday only', () => {
    const frequency = { type: 'weekly', weekdays: [1, 3, 5] }

    expect(matchesHabitFrequency(frequency, new Date(2026, 3, 20))).toBe(true)
    expect(matchesHabitFrequency(frequency, new Date(2026, 3, 21))).toBe(false)
  })

  it('matches a monthly habit on the configured day only', () => {
    const frequency = { type: 'monthly', monthDays: [1, 15, 28] }

    expect(matchesHabitFrequency(frequency, new Date(2026, 3, 15))).toBe(true)
    expect(matchesHabitFrequency(frequency, new Date(2026, 3, 16))).toBe(false)
  })

  it('treats missing frequency as daily', () => {
    expect(matchesHabitFrequency(null, new Date(2026, 3, 18))).toBe(true)
  })
})
```

- [ ] **Step 2: 运行单测并确认当前失败**

运行：

```bash
.\node_modules\.bin\vitest.CMD run src/views/habits/utils/__tests__/habitFrequency.spec.js
```

预期：

- 测试文件因 `habitFrequency.js` 不存在而失败

- [ ] **Step 3: 实现最小频率工具**

创建 `src/views/habits/utils/habitFrequency.js`：

```js
const DAILY_FREQUENCY = Object.freeze({ type: 'daily' })

const normalizeNumberArray = (values, { min, max }) => {
  if (!Array.isArray(values)) return []

  return Array.from(
    new Set(
      values
        .map((value) => Number.parseInt(value, 10))
        .filter((value) => Number.isInteger(value) && value >= min && value <= max)
    )
  ).sort((a, b) => a - b)
}

export const createDefaultHabitFrequency = () => ({ type: 'daily' })

export const getWeekdayNumber = (date) => {
  const day = date.getDay()
  return day === 0 ? 7 : day
}

export const normalizeHabitFrequency = (frequency) => {
  if (!frequency || typeof frequency !== 'object') {
    return createDefaultHabitFrequency()
  }

  if (frequency.type === 'weekly') {
    const weekdays = normalizeNumberArray(frequency.weekdays, { min: 1, max: 7 })
    return weekdays.length > 0
      ? { type: 'weekly', weekdays }
      : createDefaultHabitFrequency()
  }

  if (frequency.type === 'monthly') {
    const monthDays = normalizeNumberArray(frequency.monthDays, { min: 1, max: 31 })
    return monthDays.length > 0
      ? { type: 'monthly', monthDays }
      : createDefaultHabitFrequency()
  }

  return createDefaultHabitFrequency()
}

export const matchesHabitFrequency = (frequency, date) => {
  const normalized = normalizeHabitFrequency(frequency)

  if (normalized.type === 'daily') return true
  if (normalized.type === 'weekly') {
    return normalized.weekdays.includes(getWeekdayNumber(date))
  }
  if (normalized.type === 'monthly') {
    return normalized.monthDays.includes(date.getDate())
  }

  return true
}

export { DAILY_FREQUENCY }
```

- [ ] **Step 4: 再次运行频率工具单测并确认通过**

运行：

```bash
.\node_modules\.bin\vitest.CMD run src/views/habits/utils/__tests__/habitFrequency.spec.js
```

预期：

- `habitFrequency.spec.js` 全部通过

### Task 2：让新增习惯弹窗支持重复方式

**Files:**
- Modify: `src/views/habits/components/AddHabitModal.vue`
- Reference: `src/views/habits/utils/habitFrequency.js`

- [ ] **Step 1: 先为新增表单增加频率状态与校验计算**

在 `AddHabitModal.vue` 的 `<script setup>` 中补充导入与状态：

```js
import { computed, reactive } from 'vue'
import {
  createDefaultHabitFrequency,
  normalizeHabitFrequency
} from '@/views/habits/utils/habitFrequency'

const WEEKDAY_OPTIONS = [
  { label: '一', value: 1 },
  { label: '二', value: 2 },
  { label: '三', value: 3 },
  { label: '四', value: 4 },
  { label: '五', value: 5 },
  { label: '六', value: 6 },
  { label: '日', value: 7 }
]

const MONTH_DAY_OPTIONS = Array.from({ length: 31 }, (_, index) => index + 1)

const form = reactive({
  title: '',
  task_time: '',
  duration: 10 / 60,
  frequencyType: 'daily',
  weekdays: [],
  monthDays: []
})

const canSubmitFrequency = computed(() => {
  if (form.frequencyType === 'weekly') return form.weekdays.length > 0
  if (form.frequencyType === 'monthly') return form.monthDays.length > 0
  return true
})

const buildFrequencyPayload = () => {
  if (form.frequencyType === 'weekly') {
    return normalizeHabitFrequency({
      type: 'weekly',
      weekdays: form.weekdays
    })
  }

  if (form.frequencyType === 'monthly') {
    return normalizeHabitFrequency({
      type: 'monthly',
      monthDays: form.monthDays
    })
  }

  return createDefaultHabitFrequency()
}
```

- [ ] **Step 2: 在新增弹窗模板里加入重复方式区块**

在时间/时长区块后插入以下模板：

```vue
<div class="grid gap-3">
  <label class="text-sm font-medium leading-none">重复方式</label>

  <div class="grid grid-cols-3 gap-2">
    <Button
      type="button"
      variant="outline"
      :class="form.frequencyType === 'daily' ? 'bg-primary text-primary-foreground border-primary' : ''"
      @click="form.frequencyType = 'daily'"
    >
      每日
    </Button>
    <Button
      type="button"
      variant="outline"
      :class="form.frequencyType === 'weekly' ? 'bg-primary text-primary-foreground border-primary' : ''"
      @click="form.frequencyType = 'weekly'"
    >
      每周
    </Button>
    <Button
      type="button"
      variant="outline"
      :class="form.frequencyType === 'monthly' ? 'bg-primary text-primary-foreground border-primary' : ''"
      @click="form.frequencyType = 'monthly'"
    >
      每月
    </Button>
  </div>

  <div v-if="form.frequencyType === 'weekly'" class="grid gap-2">
    <p class="text-xs text-muted-foreground">选择每周执行的星期</p>
    <div class="grid grid-cols-7 gap-2">
      <Button
        v-for="item in WEEKDAY_OPTIONS"
        :key="item.value"
        type="button"
        variant="outline"
        :class="form.weekdays.includes(item.value) ? 'bg-primary text-primary-foreground border-primary' : ''"
        @click="toggleWeekday(item.value)"
      >
        {{ item.label }}
      </Button>
    </div>
  </div>

  <div v-if="form.frequencyType === 'monthly'" class="grid gap-2">
    <p class="text-xs text-muted-foreground">选择每月执行的日期</p>
    <div class="grid grid-cols-7 gap-2 max-h-40 overflow-y-auto pr-1">
      <Button
        v-for="day in MONTH_DAY_OPTIONS"
        :key="day"
        type="button"
        variant="outline"
        :class="form.monthDays.includes(day) ? 'bg-primary text-primary-foreground border-primary' : ''"
        @click="toggleMonthDay(day)"
      >
        {{ day }}
      </Button>
    </div>
  </div>
</div>
```

- [ ] **Step 3: 加入多选切换方法并把提交写入 frequency**

在 `AddHabitModal.vue` 中补充以下方法，并修改 `submit`：

```js
const toggleWeekday = (weekday) => {
  form.weekdays = form.weekdays.includes(weekday)
    ? form.weekdays.filter((item) => item !== weekday)
    : [...form.weekdays, weekday].sort((a, b) => a - b)
}

const toggleMonthDay = (day) => {
  form.monthDays = form.monthDays.includes(day)
    ? form.monthDays.filter((item) => item !== day)
    : [...form.monthDays, day].sort((a, b) => a - b)
}

const submit = withLoadingLock(async () => {
  if (!form.title.trim() || !canSubmitFrequency.value) return

  const userId = authStore.userId
  if (!userId) {
    console.error('User not authenticated')
    return
  }

  try {
    await db.habits.create({
      user_id: userId,
      title: form.title,
      task_time: form.task_time || null,
      duration: Math.round((Number(form.duration) || 0) * 60) || 10,
      frequency: buildFrequencyPayload(),
      target_value: 1,
      archived: false
    })

    emit('refresh')
    form.title = ''
    form.task_time = ''
    form.duration = 10 / 60
    form.frequencyType = 'daily'
    form.weekdays = []
    form.monthDays = []
    emit('update:show', false)
  } catch (e) {
    console.error('Add habit failed in modal', e)
  }
})
```

同时将提交按钮禁用条件修改为：

```vue
:disabled="!form.title.trim() || !canSubmitFrequency"
```

- [ ] **Step 4: 运行 habits 相关单测，确保新增弹窗修改未破坏现有依赖**

运行：

```bash
.\node_modules\.bin\vitest.CMD run src/views/habits/composables/__tests__/useHabitData.spec.js
```

预期：

- 现有 habits 数据层测试通过

### Task 3：让编辑习惯弹窗支持回填与保存重复方式

**Files:**
- Modify: `src/views/habits/components/EditHabitModal.vue`
- Reference: `src/views/habits/utils/habitFrequency.js`

- [ ] **Step 1: 补充编辑弹窗频率回填逻辑**

在 `EditHabitModal.vue` 的 `<script setup>` 中新增：

```js
import { computed, reactive, watch } from 'vue'
import {
  createDefaultHabitFrequency,
  normalizeHabitFrequency
} from '@/views/habits/utils/habitFrequency'

const WEEKDAY_OPTIONS = [
  { label: '一', value: 1 },
  { label: '二', value: 2 },
  { label: '三', value: 3 },
  { label: '四', value: 4 },
  { label: '五', value: 5 },
  { label: '六', value: 6 },
  { label: '日', value: 7 }
]

const MONTH_DAY_OPTIONS = Array.from({ length: 31 }, (_, index) => index + 1)

const form = reactive({
  title: '',
  task_time: '',
  duration: 10 / 60,
  frequencyType: 'daily',
  weekdays: [],
  monthDays: []
})

const canSubmitFrequency = computed(() => {
  if (form.frequencyType === 'weekly') return form.weekdays.length > 0
  if (form.frequencyType === 'monthly') return form.monthDays.length > 0
  return true
})

watch(() => props.habitData, (newVal) => {
  if (!newVal) return

  const frequency = normalizeHabitFrequency(newVal.frequency || createDefaultHabitFrequency())

  form.title = newVal.title || ''
  form.task_time = newVal.task_time ? newVal.task_time.substring(0, 5) : ''
  form.duration = (newVal.duration || 10) / 60
  form.frequencyType = frequency.type
  form.weekdays = frequency.type === 'weekly' ? [...frequency.weekdays] : []
  form.monthDays = frequency.type === 'monthly' ? [...frequency.monthDays] : []
}, { immediate: true })
```

- [ ] **Step 2: 复用与新增弹窗一致的重复方式 UI**

把 Task 2 中的“重复方式”模板块复制到 `EditHabitModal.vue` 的时间/时长区块后，保持交互一致。

- [ ] **Step 3: 更新保存逻辑与按钮禁用条件**

在 `EditHabitModal.vue` 中加入：

```js
const toggleWeekday = (weekday) => {
  form.weekdays = form.weekdays.includes(weekday)
    ? form.weekdays.filter((item) => item !== weekday)
    : [...form.weekdays, weekday].sort((a, b) => a - b)
}

const toggleMonthDay = (day) => {
  form.monthDays = form.monthDays.includes(day)
    ? form.monthDays.filter((item) => item !== day)
    : [...form.monthDays, day].sort((a, b) => a - b)
}

const buildFrequencyPayload = () => {
  if (form.frequencyType === 'weekly') {
    return normalizeHabitFrequency({
      type: 'weekly',
      weekdays: form.weekdays
    })
  }

  if (form.frequencyType === 'monthly') {
    return normalizeHabitFrequency({
      type: 'monthly',
      monthDays: form.monthDays
    })
  }

  return createDefaultHabitFrequency()
}
```

并把 `submit` 改成：

```js
const submit = withLoadingLock(async () => {
  if (!form.title.trim() || !props.habitData?.id || !canSubmitFrequency.value) return

  try {
    await db.habits.update(props.habitData.id, {
      title: form.title,
      task_time: form.task_time || null,
      duration: Math.round((Number(form.duration) || 0) * 60) || 10,
      frequency: buildFrequencyPayload()
    })
    emit('refresh')
    emit('update:show', false)
  } catch (e) {
    console.error('Update habit failed in modal', e)
  }
})
```

提交按钮禁用条件更新为：

```vue
:disabled="!form.title.trim() || !canSubmitFrequency"
```

- [ ] **Step 4: 运行 habits 相关单测，确认编辑弹窗接入未破坏数据层**

运行：

```bash
.\node_modules\.bin\vitest.CMD run src/views/habits/composables/__tests__/useHabitData.spec.js
```

预期：

- 现有 habits 数据层测试继续通过

### Task 4：让 day 页按当天日期过滤习惯

**Files:**
- Modify: `src/views/day/composables/useDayData.js`
- Reference: `src/views/habits/utils/habitFrequency.js`

- [ ] **Step 1: 先为 day 页补一个基于频率过滤的单测**

在 `src/views/day/composables/__tests__/useDayNavigation.spec.js` 之外，新增 `src/views/habits/utils/__tests__/habitFrequency.spec.js` 已覆盖核心日期判断，本任务不再重复新增大体量 day 页集成测试，先复用工具层测试作为逻辑保障，并只做最小集成修改。

- [ ] **Step 2: 在 day 数据层接入 matchesHabitFrequency**

修改 `src/views/day/composables/useDayData.js`，补充导入：

```js
import { matchesHabitFrequency } from '@/views/habits/utils/habitFrequency'
```

并把 habits 过滤从：

```js
habits.value = allHabits.filter(h => !h.is_archived && h.task_time)
```

改为：

```js
habits.value = allHabits.filter((habit) => {
  return !habit.is_archived
    && habit.task_time
    && matchesHabitFrequency(habit.frequency, startOfDay)
})
```

- [ ] **Step 3: 运行频率工具单测，确认 day 页依赖的规则判断仍然稳定**

运行：

```bash
.\node_modules\.bin\vitest.CMD run src/views/habits/utils/__tests__/habitFrequency.spec.js
```

预期：

- 频率工具测试通过，day 页所依赖的重复规则判断稳定

### Task 5：补充服务层与数据库结构文档说明

**Files:**
- Modify: `src/services/db/habits.js`
- Modify: `database/current-structure.md`

- [ ] **Step 1: 在 habits 服务层注释中补充 frequency 运行时约定**

将 `src/services/db/habits.js` 顶部说明扩展为：

```js
/**
 * 【frequency 约定】
 * - daily:   { type: 'daily' }
 * - weekly:  { type: 'weekly', weekdays: [1, 3, 5] }
 * - monthly: { type: 'monthly', monthDays: [1, 15, 28] }
 * - 老数据缺失 frequency 时，运行时按 daily 兼容
 */
```

- [ ] **Step 2: 在数据库结构快照中补充 frequency 的运行时说明**

在 `database/current-structure.md` 的 `public.habits` 小节说明中追加：

```md
- `frequency` 当前运行时约定为 JSON 结构：
  - `{ "type": "daily" }`
  - `{ "type": "weekly", "weekdays": [1, 3, 5] }`
  - `{ "type": "monthly", "monthDays": [1, 15, 28] }`
```

- [ ] **Step 3: 运行本次相关单测集合，做收口验证**

运行：

```bash
.\node_modules\.bin\vitest.CMD run src/views/habits/utils/__tests__/habitFrequency.spec.js src/views/habits/composables/__tests__/useHabitData.spec.js
```

预期：

- 本次新增的频率工具测试通过
- 现有 habits 数据层测试继续通过

## 自检结论

### Spec 覆盖

已覆盖以下规格要求：

1. 新增习惯可配置 `每日 / 每周 / 每月`
2. 编辑习惯可回填并修改重复方式
3. `day` 页只展示当天命中的习惯
4. 老数据缺失 `frequency` 时默认按 `daily` 兼容
5. 不引入第三方重复规则库，不新增复杂表结构

### 占位词检查

本计划未使用 `TBD`、`TODO`、`后续补充`、`类似任务 N` 等占位表达。

### 一致性检查

本计划统一使用以下字段与方法名：

- `frequency.type`
- `frequency.weekdays`
- `frequency.monthDays`
- `normalizeHabitFrequency`
- `matchesHabitFrequency`
- `createDefaultHabitFrequency`

未出现前后命名不一致问题。
