# 全量代码风格重构 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 清理冗余注释、删除移动端代码、消除透传层、修复 Direction 模块 Pinia 架构

**Architecture:** 按依赖链顺序执行——先清噪音（注释+移动端），再改架构（useDayData+Direction）。每阶段完成后 `pnpm build` 验证。

**Tech Stack:** Vue 3 Composition API, Pinia, Tailwind CSS 4, Vite 7

---

## 文件结构总览

### 删除的文件
- `src/composables/useMobile.js`
- `src/views/habits/HabitsMobilePage.vue`
- `src/views/habits/HabitMobileListSheet.vue`
- `src/views/day/components/MobileAddEventDrawer.vue`
- `src/views/day/utils/mobileLayers.js`
- `src/views/day/utils/__tests__/mobileLayers.spec.js`
- `src/views/day/composables/useSwipeToComplete.js`
- `src/views/day/composables/useDayData.js`
- `src/views/direction/composables/useDirectionState.js`

### 重大修改的文件
- `src/views/day/index.vue` — 删除移动端分支 + 删除 useDayData + 子组件自管理
- `src/views/day/components/Sidebar.vue` — 删除移动端分支 + 自管理 hooks
- `src/views/day/components/Timeline.vue` — 自管理 hooks
- `src/stores/directionStore.js` — 吸收 useDirectionState 全部状态
- 6 个 direction composables — 改 import 来源
- 4 个 direction components — 改 import 来源
- 3 个 direction test files — 改 import 来源

---

## Task 1: 注释精简 — 全局 composables 与 utils

**Files:**
- Modify: `src/composables/useGlobalLoading.js`
- Modify: `src/composables/useNotifications.js`
- Modify: `src/composables/useResizable.js`
- Modify: `src/composables/useToast.js`
- Modify: `src/utils/dateFormatter.js`
- Modify: `src/utils/audio.js`
- Modify: `src/utils/goalDayStatus.js`
- Modify: `src/lib/utils.ts`

**注释标准：**
- 删除：变量名已清晰的 JSDoc、重复代码含义的注释、文件头大段 HTML 注释
- 保留：解释非显然约束的行内注释、关键函数的 @param/@returns（参数含义不显然时）、分组分隔线

- [ ] **Step 1: 清理 `useGlobalLoading.js`**

删除每个变量的 JSDoc（`/** 延迟显示阈值... */`、`/** 当前pending中的请求数量 */` 等），只保留常量的简短行内注释：

```js
// 延迟阈值：防止极短请求导致闪烁
const SHOW_DELAY_MS = 200
// 最小可见时长：避免 loading 闪烁
const MIN_VISIBLE_MS = 300

const pendingCount = ref(0)
const visible = ref(false)
let showTimer = null
let hideTimer = null
let visibleAt = 0
```

删除文件顶部的 `/** ... */` 大段 JSDoc 块。

- [ ] **Step 2: 清理 `useNotifications.js`**

删除文件头的 24 行 JSDoc 块和函数级重复的 JSDoc 块（行 29-35）。保留 `startListening` 中解释重入保护的注释。

- [ ] **Step 3: 清理 `useResizable.js`**

删除每个函数的冗长 `@description`、`@example` 块，保留 `@param` 和 `@returns`（参数类型不显然时）。保留行 129 的 `// 先移除可能残留的旧监听器，防止快速 mousedown 叠加`。

- [ ] **Step 4: 清理 `useToast.js`、`dateFormatter.js`、`audio.js`、`goalDayStatus.js`、`utils.ts`**

按同样标准清理：删除重复说明、保留有价值的约束注释。

- [ ] **Step 5: 验证构建**

Run: `pnpm build`
Expected: 构建成功，无报错

---

## Task 2: 注释精简 — Stores

**Files:**
- Modify: `src/stores/authStore.js`
- Modify: `src/stores/dateStore.js`
- Modify: `src/stores/dayStore.js`
- Modify: `src/stores/uiStore.js`
- Modify: `src/stores/pomodoroStore.js`
- Modify: `src/stores/onboardingStore.js`
- Modify: `src/stores/pageStateStore.js`
- Modify: `src/stores/directionStore.js`

- [ ] **Step 1: 清理 `authStore.js`**

删除文件头大段 JSDoc。保留 `@example`（如果用法不显然）。保留解释 `persist: true` 行为的注释。

- [ ] **Step 2: 清理 `dateStore.js`**

保留行 58 的 `// 注意：JavaScript 中的 month 是 0-indexed 的`。删除其他冗余注释。

- [ ] **Step 3: 清理 `dayStore.js`**

删除 `/** 更新任务的开始/结束时间（拖拽编排使用） */` 等重复函数名的注释。保留 `fetchTasks` 中解释并行请求策略的注释（如果有）。

- [ ] **Step 4: 清理其余 stores**

按同样标准清理 `uiStore.js`、`pomodoroStore.js`、`onboardingStore.js`、`pageStateStore.js`、`directionStore.js`。

- [ ] **Step 5: 验证构建**

Run: `pnpm build`
Expected: 构建成功

---

## Task 3: 注释精简 — Day 模块

**Files:**
- Modify: `src/views/day/index.vue`
- Modify: `src/views/day/components/Sidebar.vue`
- Modify: `src/views/day/components/Timeline.vue`
- Modify: `src/views/day/components/TaskItemWrapper.vue`
- Modify: `src/views/day/components/TaskItem.vue`
- Modify: `src/views/day/components/DailyReportModal.vue`
- Modify: `src/views/day/components/PomodoroTimerModal.vue`
- Modify: `src/views/day/components/TimelineMarker.vue`
- Modify: `src/views/day/components/AddEventModal.vue`
- Modify: `src/views/day/composables/useDayNavigation.js`
- Modify: `src/views/day/composables/useDayModal.js`
- Modify: `src/views/day/composables/useDailyReport.js`
- Modify: `src/views/day/composables/useDayExecutionItems.js`
- Modify: `src/views/day/utils/routeDateContext.js`
- Modify: `src/views/day/utils/timelineLayout.js`
- Modify: `src/views/day/utils/sidebarSections.js`
- Modify: `src/views/day/utils/getInitialScrollTarget.js`

- [ ] **Step 1: 清理 `day/index.vue`**

删除整个文件头 HTML 注释（行 1-22 的 `<!-- ... -->` 块）。删除 `<script>` 中的大段 JSDoc（行 129-164）。保留 `handleDailyReportCarryover` 中的 try/catch 结构注释（如果有）。

- [ ] **Step 2: 清理 `Sidebar.vue`**

删除文件头 HTML 注释。删除 `<script>` 中的 JSDoc 块（行 286-310）。删除 Props 的 JSDoc（行 327-332）。保留分组分隔线 `// ==== 状态初始化 ====` 等。保留 `useResizable` 的简短说明。

- [ ] **Step 3: 清理 `Timeline.vue`**

删除文件头 HTML 注释（行 2-37 的大块）。删除 `<script>` 中的冗长 `displaySchedule` JSDoc（行 129-146），改为简短一行：`// 贪心列分配：处理重叠日程的并排布局`。保留 Props 的简短注释。

- [ ] **Step 4: 清理其余 day 模块文件**

按同样标准清理所有 composables、utils 和组件文件。重点删除：
- `useDayNavigation.js` 中每个函数的冗长 JSDoc
- `useDayModal.js` 中重复函数名的注释
- `useDayExecutionItems.js` 中重复代码逻辑的注释

- [ ] **Step 5: 验证构建**

Run: `pnpm build`
Expected: 构建成功

---

## Task 4: 注释精简 — 其他模块

**Files:**
- Modify: `src/views/habits/index.vue`
- Modify: `src/views/habits/HabitsDesktopPage.vue`
- Modify: `src/views/habits/composables/*.js`
- Modify: `src/views/direction/index.vue`
- Modify: `src/views/direction/composables/*.js`
- Modify: `src/views/direction/components/*.vue`
- Modify: `src/views/summary/index.vue`
- Modify: `src/views/summary/components/*.vue`
- Modify: `src/views/summary/composables/*.js`
- Modify: `src/views/month/*.vue`
- Modify: `src/views/year/*.vue`
- Modify: `src/views/login/*.vue`
- Modify: `src/components/*.vue`
- Modify: `src/router/index.js`

- [ ] **Step 1: 清理 `summary/index.vue`**

删除 `</template>` 和 `<script>` 之间的 44 行死文档（行 76-93 的 `/** ... */` 块，位于 template 和 script 之间）。

- [ ] **Step 2: 清理 habits 模块**

清理 `index.vue`（将在 Task 5 中大幅重写，此处只清理 `HabitsDesktopPage.vue` 和 composables）。删除文件头 HTML 注释和冗余 JSDoc。

- [ ] **Step 3: 清理 direction 模块**

清理 `index.vue`、所有 composables、所有 components 的文件头注释和冗余 JSDoc。

- [ ] **Step 4: 清理 summary、month、year、login 模块**

按同样标准清理所有文件。

- [ ] **Step 5: 清理 components 和 router**

清理 `src/components/` 下的共享组件和 `src/router/index.js`。

- [ ] **Step 6: 验证构建**

Run: `pnpm build`
Expected: 构建成功

---

## Task 5: 删除移动端代码

**Files:**
- Delete: `src/composables/useMobile.js`
- Delete: `src/views/habits/HabitsMobilePage.vue`
- Delete: `src/views/habits/HabitMobileListSheet.vue`
- Delete: `src/views/day/components/MobileAddEventDrawer.vue`
- Delete: `src/views/day/utils/mobileLayers.js`
- Delete: `src/views/day/utils/__tests__/mobileLayers.spec.js`
- Delete: `src/views/day/composables/useSwipeToComplete.js`
- Modify: `src/views/day/index.vue`
- Modify: `src/views/day/components/Sidebar.vue`
- Modify: `src/views/habits/index.vue`
- Modify: `src/views/summary/components/SummarySidebar.vue`

- [ ] **Step 1: 删除 7 个移动端文件**

```bash
rm src/composables/useMobile.js
rm src/views/habits/HabitsMobilePage.vue
rm src/views/habits/HabitMobileListSheet.vue
rm src/views/day/components/MobileAddEventDrawer.vue
rm src/views/day/utils/mobileLayers.js
rm src/views/day/utils/__tests__/mobileLayers.spec.js
rm src/views/day/composables/useSwipeToComplete.js
```

- [ ] **Step 2: 重写 `habits/index.vue`**

删除 `useMobile` 引用和 `HabitsMobilePage` 引用，直接渲染 `HabitsDesktopPage`：

```vue
<template>
  <HabitsDesktopPage />
</template>

<script setup>
import HabitsDesktopPage from '@/views/habits/HabitsDesktopPage.vue'
</script>

<style scoped>
@reference "@/assets/tw-theme.css";
</style>
```

- [ ] **Step 3: 重写 `day/index.vue` — 删除移动端分支**

删除以下内容：
- `import { useMobile } from '@/composables/useMobile'`
- `import { getMobileOverlayClass, getMobileOverlayStyle, getMobileSidebarShellClass, getSidebarMotionClass, MOBILE_SIDEBAR_WIDTH } from '@/views/day/utils/mobileLayers'`
- `const AddEventModal = defineAsyncComponent(() => import('@/views/day/components/AddEventModal.vue'))` 的 `!isMobile` 条件判断
- `const MobileAddEventDrawer = defineAsyncComponent(...)` 整行
- `const { isMobile } = useMobile()`
- `showSidebar` ref 和相关 watch
- 模板中的：mobile overlay div、mobile sidebar shell div、mobile FAB 按钮（2 个）、`v-if="isMobile"` / `v-else` 分支
- `watch([showSidebar, isMobile], ...)` 和 `uiStore.setNavbarHidden` 逻辑

重构后模板只保留：
```vue
<template>
  <div class="h-screen w-full bg-background flex overflow-hidden font-sans text-foreground relative selection:bg-primary selection:text-primary-foreground">
    <div class="flex flex-1 h-full relative overflow-hidden bg-zinc-50/50">
      <Sidebar
        :is-loading="isLoading"
        @scroll-to-task="scrollToTask"
        @add-event="openAddModal"
        @edit-task="openEditModal"
      />

      <div class="relative flex-1 flex flex-col overflow-hidden">
        <Timeline
          ref="timeline"
          :class="['transition-all duration-300 ease-in-out flex-1', isReady ? 'opacity-100' : 'opacity-0']"
          :current-hour="currentHour"
          :is-loading="isLoading"
          @edit-task="openEditModal"
          @select-task="scrollToTask"
        />
      </div>

      <AddEventModal
        v-model:show="showAddModal"
        :initial-data="editingTask"
      />

      <DailyReportModal
        :show="reportVisible"
        :stats="reportStats"
        @close="closeReport"
        @confirm="handleDailyReportConfirm"
        @confirm-carryover="handleDailyReportCarryover"
      />

      <PomodoroTimerModal />
    </div>
  </div>
</template>
```

script 中删除 `Menu`、`Plus` 的 import（不再需要图标），删除 `showSidebar` ref，删除 mobile 相关 watch。保留 `useDayNavigation`、`useDayModal`、`useDailyReport`、`useDayData`（useDayData 将在 Task 6 中删除）、`useNotifications`、`useUiStore`、`useDateStore` 的引用。

- [ ] **Step 4: 重写 `Sidebar.vue` — 删除移动端分支**

删除以下内容：
- `isMobile` prop
- `import { useSwipeToComplete } from '...'`
- `import { getSidebarPanelClass } from '...'`
- touch handler 相关代码（`swipeState`、`getSwipeOffset`、`handleTouchStart` 等）
- 模板中 `v-if="isMobile"` 的整个移动分支（行 44-158）
- 模板中 `v-if="isMobile"` 的关闭按钮（行 27-35）
- `:style="{ width: isMobile ? '100%' : width + 'px' }"` 改为 `:style="{ width: width + 'px' }"`
- `:class="getSidebarPanelClass({ isMobile, show })"` 改为固定桌面端 class

保留：桌面端任务列表（行 162-250 的 `v-else-if="!isMobile"` 分支，去掉条件变成默认渲染）、resize handle、footer。

- [ ] **Step 5: 更新 `SummarySidebar.vue` — 删除移动端逻辑**

删除：
- `import { useMobile } from '@/composables/useMobile'`
- `const { isMobile } = useMobile()`
- `:style="isMobile ? { width: '100%' } : { width: width + 'px' }"` 改为 `:style="{ width: width + 'px' }"`
- `v-if="!isMobile"` 条件（resize handle 始终显示）

删除 `</template>` 和 `<script>` 之间的 JSDoc 块（行 76-93）。

- [ ] **Step 6: 验证构建**

Run: `pnpm build`
Expected: 构建成功

Run: `pnpm test:unit`（如果存在）
Expected: mobileLayers 测试已删除，其余测试通过

---

## Task 6: 删除 useDayData 透传层 + 子组件自管理 hooks

**Files:**
- Delete: `src/views/day/composables/useDayData.js`
- Modify: `src/views/day/composables/useDayNavigation.js`
- Modify: `src/views/day/composables/useDayModal.js`
- Modify: `src/views/day/index.vue`
- Modify: `src/views/day/components/Sidebar.vue`
- Modify: `src/views/day/components/Timeline.vue`

- [ ] **Step 1: 删除 `useDayData.js`**

```bash
rm src/views/day/composables/useDayData.js
```

- [ ] **Step 2: 重写 `useDayNavigation.js` — 直接用 `dayStore`**

将 `import { useDayData } from './useDayData'` 改为 `import { useDayStore } from '@/stores/dayStore'`。将 `const { dailySchedule, fetchTasks, isLoading } = useDayData()` 改为：

```js
const dayStore = useDayStore()
```

后续引用改为 `dayStore.dailySchedule`、`dayStore.fetchTasks`、`dayStore.isLoading`。

返回值保持不变：`{ currentHour, isReady, isLoading: dayStore.isLoading, scrollToTask, updateCurrentHour, validateDayRoute }`

- [ ] **Step 3: 重写 `useDayModal.js` — 直接用 `dayStore`**

将 `import { useDayData } from './useDayData'` 改为 `import { useDayStore } from '@/stores/dayStore'`。将 `const { dailySchedule } = useDayData()` 改为 `const dayStore = useDayStore()`。`dailySchedule` 引用改为 `dayStore.dailySchedule`。

- [ ] **Step 4: 更新 `day/index.vue` — 直接用 `dayStore`**

将 `import { useDayData } from '...'` 改为 `import { useDayStore } from '@/stores/dayStore'`。将 `const { dailySchedule, carryOverUncompletedTasksTo, fetchTasks, setLoading } = useDayData()` 改为 `const dayStore = useDayStore()`。后续引用改为 `dayStore.xxx`。

- [ ] **Step 5: 更新 `Sidebar.vue` — 自管理 hooks**

将 `import { useDayData } from '...'` 改为 `import { useDayStore } from '@/stores/dayStore'`。将 `const { dailySchedule, completedCount, handleToggleComplete } = useDayData()` 改为 `const dayStore = useDayStore()`。

删除 `isMobile` prop（已在 Task 5 中删除）、`show` prop（不再需要，桌面端始终显示）。保留 `isLoading` prop 或改为从 store 直接取。

删除 `defineEmits(['scrollToTask', 'add-event', 'edit-task', 'close'])` 中的 `'close'`（不再需要）。

- [ ] **Step 6: 更新 `Timeline.vue` — 自管理 hooks**

将 `import { useDayData } from '...'` 改为 `import { useDayStore } from '@/stores/dayStore'`。将 `const { dailySchedule } = useDayData()` 改为 `const dayStore = useDayStore()`。

删除 `currentHour` 和 `isLoading` props，改为从 `useDayNavigation` 或 `dayStore` 直接获取。但这会引入 `useDayNavigation` 的路由逻辑到 Timeline，可能不合适。**保留 props**——`currentHour` 和 `isLoading` 是合理的父子通信，不是 hook 透传。

- [ ] **Step 7: 验证构建**

Run: `pnpm build`
Expected: 构建成功

---

## Task 7: 修复 Direction 模块 Pinia 架构

**Files:**
- Modify: `src/stores/directionStore.js` — 重写，吸收全部状态
- Delete: `src/views/direction/composables/useDirectionState.js`
- Modify: `src/views/direction/composables/useDirectionBatch.js` — 改 import
- Modify: `src/views/direction/composables/useDirectionTasks.js` — 改 import
- Modify: `src/views/direction/composables/useDirectionSelection.js` — 改 import
- Modify: `src/views/direction/composables/useDirectionGoals.js` — 改 import
- Modify: `src/views/direction/composables/useDirectionFetch.js` — 改 import
- Modify: `src/views/direction/components/GoalRangePicker.vue` — 改 import
- Modify: `src/views/direction/components/DirectionSidebar.vue` — 改 import
- Modify: `src/views/direction/components/AddGoalModal.vue` — 改 import
- Modify: `src/views/direction/components/CategoryManagementModal.vue` — 改 import
- Modify: `src/views/direction/composables/__tests__/useDirectionGoals.spec.js` — 改 import
- Modify: `src/views/direction/composables/__tests__/useDirectionFetch.spec.js` — 改 import
- Modify: `src/views/direction/composables/__tests__/useDirectionBatch.spec.js` — 改 import

- [ ] **Step 1: 重写 `directionStore.js`**

将 `useDirectionState.js` 中的所有状态定义搬入 store。新的 `directionStore.js`：

```js
import { defineStore } from 'pinia'
import { ref, reactive } from 'vue'
import { getMonthName } from '@/utils/dateFormatter'

export const months = Array.from({ length: 12 }, (_, i) => ({
  label: getMonthName(i + 1, 'zh'),
  value: i + 1,
  full: getMonthName(i + 1, 'full')
}))

export const useDirectionStore = defineStore('direction', () => {
  // 数据状态
  const goals = ref([])
  const goalMonths = ref([])
  const goalMonthsCache = reactive({})
  const goalDaysCache = reactive({})
  const archiveVersion = ref(0)

  // 选择状态
  const selectedGoal = ref(null)
  const editingGoal = ref(null)
  const selectedMonth = ref(null)
  const activePicker = ref('start')
  const isSelecting = ref(false)

  // UI 状态
  const showAddModal = ref(false)
  const showCategoryModal = ref(false)

  // 批量编辑状态
  const goalMonthsMap = reactive({})
  const dailyTasks = reactive({})
  const selectedDates = reactive({})
  const batchInput = ref('')

  // 初始化状态
  const initialized = ref(false)

  // 分类数据
  const categories = ref([])

  // 辅助方法
  const getGoalMonthsByGoalId = (goalId) => goalMonthsCache[goalId] || []

  const clearGoalDaysCache = (keepMonthPlanId = null) => {
    if (keepMonthPlanId && goalDaysCache[keepMonthPlanId]) {
      const keepData = goalDaysCache[keepMonthPlanId]
      Object.keys(goalDaysCache).forEach(key => {
        delete goalDaysCache[key]
      })
      goalDaysCache[keepMonthPlanId] = keepData
    } else {
      Object.keys(goalDaysCache).forEach(key => {
        delete goalDaysCache[key]
      })
    }
  }

  const syncGoalMonthsToFlatList = (goalId) => {
    const cached = goalMonthsCache[goalId] || []
    const others = goalMonths.value.filter(item => item.goal_id !== goalId)
    goalMonths.value = [...others, ...cached]
  }

  const reset = () => {
    goals.value = []
    goalMonths.value = []
    editingGoal.value = null
    selectedGoal.value = null
    selectedMonth.value = null
    activePicker.value = 'start'
    isSelecting.value = false
    showAddModal.value = false
    showCategoryModal.value = false
    initialized.value = false
    categories.value = []

    Object.keys(goalMonthsCache).forEach(key => delete goalMonthsCache[key])
    Object.keys(goalDaysCache).forEach(key => delete goalDaysCache[key])
    Object.keys(goalMonthsMap).forEach(key => delete goalMonthsMap[key])
    Object.keys(dailyTasks).forEach(key => delete dailyTasks[key])
    Object.keys(selectedDates).forEach(key => delete selectedDates[key])
    batchInput.value = ''
    archiveVersion.value = 0
  }

  return {
    // 数据
    goals, goalMonths, goalMonthsCache, goalDaysCache, archiveVersion,
    // 选择
    selectedGoal, editingGoal, selectedMonth, activePicker, isSelecting,
    // UI
    showAddModal, showCategoryModal,
    // 批量编辑
    goalMonthsMap, dailyTasks, selectedDates, batchInput,
    // 初始化
    initialized,
    // 分类
    categories,
    // 方法
    getGoalMonthsByGoalId, clearGoalDaysCache, syncGoalMonthsToFlatList, reset
  }
})
```

- [ ] **Step 2: 删除 `useDirectionState.js`**

```bash
rm src/views/direction/composables/useDirectionState.js
```

- [ ] **Step 3: 更新 6 个 composables 的 import**

每个文件将 `import { ... } from '@/views/direction/composables/useDirectionState'` 改为 `import { useDirectionStore } from '@/stores/directionStore'`，然后通过 `const store = useDirectionStore()` 访问状态。

**`useDirectionBatch.js`** (行 29):
```js
// 之前
import { selectedDates, dailyTasks, goalDaysCache, ... } from '@/views/direction/composables/useDirectionState'
// 之后
import { useDirectionStore } from '@/stores/directionStore'
// 在函数内部
const store = useDirectionStore()
// 引用改为 store.selectedDates, store.dailyTasks, store.goalDaysCache 等
```

**`useDirectionTasks.js`** (行 2):
```js
// 之前
import { dailyTasks } from '@/views/direction/composables/useDirectionState'
// 之后
import { useDirectionStore } from '@/stores/directionStore'
const store = useDirectionStore()
// 引用改为 store.dailyTasks
```

**`useDirectionSelection.js`** (行 13):
```js
import { useDirectionStore } from '@/stores/directionStore'
const store = useDirectionStore()
// 引用改为 store.selectedGoal, store.editingGoal 等
```

**`useDirectionGoals.js`** (行 17):
```js
import { useDirectionStore } from '@/stores/directionStore'
const store = useDirectionStore()
// 引用改为 store.goals, store.goalMonths 等
```

**`useDirectionFetch.js`** (行 18):
```js
import { useDirectionStore } from '@/stores/directionStore'
const store = useDirectionStore()
// 引用改为 store.goals, store.initialized, store.categories 等
```

注意：这些 composable 内部使用了 `useDirectionState` 导出的模块级 refs，现在需要改为从 store 实例获取。如果 composable 是在 `defineStore` 外部调用的，需要确保 `useDirectionStore()` 在 composable 函数内部调用（而不是模块顶层），因为 Pinia store 需要在 setup 上下文中使用。

- [ ] **Step 4: 更新 4 个 components 的 import**

**`GoalRangePicker.vue`** (行 73):
```js
// 之前
import { goalMonthsCache } from '@/views/direction/composables/useDirectionState'
// 之后
import { useDirectionStore } from '@/stores/directionStore'
const store = useDirectionStore()
// 引用改为 store.goalMonthsCache
```

**`DirectionSidebar.vue`** (行 81):
```js
// 之前
import { goalMonths, dailyTasks, getGoalMonthsByGoalId } from '@/views/direction/composables/useDirectionState'
// 之后
import { useDirectionStore } from '@/stores/directionStore'
const store = useDirectionStore()
// 引用改为 store.goalMonths, store.dailyTasks, store.getGoalMonthsByGoalId
```

**`AddGoalModal.vue`** (行 163, 168):
```js
// 之前
import { months } from '@/views/direction/composables/useDirectionState'
import { categories } from '@/views/direction/composables/useDirectionState'
// 之后
import { months, useDirectionStore } from '@/stores/directionStore'
const store = useDirectionStore()
// categories 引用改为 store.categories
// months 是常量，直接从 store 文件 import
```

**`CategoryManagementModal.vue`** (行 54):
```js
// 之前
import { showCategoryModal, categories } from '@/views/direction/composables/useDirectionState'
// 之后
import { useDirectionStore } from '@/stores/directionStore'
const store = useDirectionStore()
// 引用改为 store.showCategoryModal, store.categories
```

- [ ] **Step 5: 更新 3 个 test files 的 import**

**`useDirectionGoals.spec.js`** (行 8):
```js
// 之前
import { ... } from '@/views/direction/composables/useDirectionState'
// 之后
import { useDirectionStore } from '@/stores/directionStore'
// 测试中通过 store 实例访问状态
```

**`useDirectionFetch.spec.js`** (行 15): 同上模式

**`useDirectionBatch.spec.js`** (行 10): 同上模式

- [ ] **Step 6: 验证构建**

Run: `pnpm build`
Expected: 构建成功

Run: `pnpm test:unit`
Expected: 所有测试通过

---

## Task 8: 最终验证

- [ ] **Step 1: 完整构建验证**

Run: `pnpm build`
Expected: 构建成功，无警告

- [ ] **Step 2: 类型检查**

Run: `pnpm vue-tsc`
Expected: 无类型错误

- [ ] **Step 3: 运行测试**

Run: `pnpm test:unit`
Expected: 所有测试通过

- [ ] **Step 4: 检查无残留引用**

Run: `grep -r "useMobile" src/ --include="*.vue" --include="*.js" --include="*.ts"`
Expected: 无结果

Run: `grep -r "useDayData" src/ --include="*.vue" --include="*.js" --include="*.ts"`
Expected: 无结果

Run: `grep -r "useDirectionState" src/ --include="*.vue" --include="*.js" --include="*.ts"`
Expected: 无结果

Run: `grep -r "mobileLayers" src/ --include="*.vue" --include="*.js" --include="*.ts"`
Expected: 无结果

Run: `grep -r "useSwipeToComplete" src/ --include="*.vue" --include="*.js" --include="*.ts"`
Expected: 无结果
